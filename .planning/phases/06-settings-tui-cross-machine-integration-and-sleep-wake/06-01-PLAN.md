---
phase: 06-settings-tui-cross-machine-integration-and-sleep-wake
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/tui/settings/fields.rs
  - tests/e2e/sandbox.rs
  - tests/web_session_create.rs
autonomous: true
requirements: [RT-01, INT-01, SET-01, SET-02, TEST-04]
must_haves:
  truths:
    - "Sbx-incompatible fields (ExtraVolumes, VolumeIgnores, MountSsh) do not appear in the settings list when the resolved runtime is Sbx"
    - "All 13 sandbox fields appear for Docker/Podman/AppleContainer runtimes (no regression)"
    - "A cross-product test asserts that field applicability matches the capability matrix for every FieldKey x ContainerRuntimeName combination"
    - "Web dashboard and cockpit operate transparently with sbx runtime (no sbx-specific code paths in src/server/ or src/cockpit/)"
    - "Sbx appears in the runtime selector TUI and selection persists across restarts"
  artifacts:
    - path: "src/tui/settings/fields.rs"
      provides: "is_field_applicable predicate + filter in build_sandbox_fields + cross-product test"
      contains: "fn is_field_applicable"
    - path: "tests/e2e/sandbox.rs"
      provides: "Sbx runtime selector e2e test"
      contains: "sbx_runtime_selector"
    - path: "tests/web_session_create.rs"
      provides: "Web API sbx session integration test"
      contains: "web_session_create_sbx"
  key_links:
    - from: "src/tui/settings/fields.rs"
      to: "src/containers/runtime_base.rs"
      via: "RuntimeBase::{variant}.capabilities import"
      pattern: "RuntimeBase::SBX\\.capabilities"
    - from: "src/tui/settings/fields.rs"
      to: "src/containers/container_interface.rs"
      via: "RuntimeCapabilities type import"
      pattern: "use crate::containers::container_interface::RuntimeCapabilities"
---

<objective>
Wire capability-driven field visibility gating into the settings TUI, add the cross-product correctness test, verify cross-surface transparency for sbx, and add e2e test coverage for the sbx runtime selector.

Purpose: Ensures that settings fields irrelevant to sbx are hidden (not shown), that existing runtimes see no regression, and that all integration surfaces (web, cockpit, TUI) work transparently with sbx selected.

Output: Modified `build_sandbox_fields()` with filter predicate, in-module cross-product test, sbx runtime selector e2e test, web session integration test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-settings-tui-cross-machine-integration-and-sleep-wake/06-CONTEXT.md
@.planning/phases/06-settings-tui-cross-machine-integration-and-sleep-wake/06-RESEARCH.md
@.planning/phases/06-settings-tui-cross-machine-integration-and-sleep-wake/06-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/containers/container_interface.rs:
```rust
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub struct RuntimeCapabilities {
    pub supports_read_only_volumes: bool,
    pub supports_remove_volumes: bool,
    pub supports_port_publish_at_create: bool,
    pub supports_image_pull: bool,
    pub supports_anonymous_volumes: bool,
    pub supports_arbitrary_volume_paths: bool,
    pub supports_dynamic_port_publish: bool,
}
```

From src/containers/runtime_base.rs:
```rust
impl RuntimeBase {
    pub const DOCKER: Self = Self { ... capabilities: RuntimeCapabilities { ... } };
    pub const PODMAN: Self = Self { ... };
    pub const APPLE_CONTAINER: Self = Self { ... };
    pub const SBX: Self = Self { ... capabilities: RuntimeCapabilities {
        supports_read_only_volumes: false,
        supports_remove_volumes: false,
        supports_port_publish_at_create: false,
        supports_image_pull: false,
        supports_anonymous_volumes: false,
        supports_arbitrary_volume_paths: false,
        supports_dynamic_port_publish: true,
    }};
}
```

From src/tui/settings/fields.rs (relevant FieldKey variants):
```rust
pub enum FieldKey {
    // Sandbox
    SandboxEnabledByDefault, DefaultImage, Environment, SandboxAutoCleanup,
    CpuLimit, MemoryLimit, DefaultTerminalMode, ExtraVolumes, PortMappings,
    VolumeIgnores, MountSsh, CustomInstruction, ContainerRuntime,
    // ...
}
```

From src/session/config.rs:
```rust
pub enum ContainerRuntimeName { Docker, Podman, AppleContainer, Sbx }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add capability-driven field filter to build_sandbox_fields and cross-product test</name>
  <files>src/tui/settings/fields.rs</files>
  <read_first>
    - src/tui/settings/fields.rs (full file; understand build_sandbox_fields at line 851, FieldKey enum at line 50, existing test module)
    - src/containers/container_interface.rs (RuntimeCapabilities struct at line 63)
    - src/containers/runtime_base.rs (RuntimeBase::SBX constant and its capabilities)
  </read_first>
  <action>
    Add two imports at the top of src/tui/settings/fields.rs:
    - `use crate::containers::container_interface::RuntimeCapabilities;`
    - `use crate::containers::runtime_base::RuntimeBase;`

    Add a free function `fn is_field_applicable(key: FieldKey, caps: &RuntimeCapabilities) -> bool` below the existing `resolve_optional` helper (before `build_sandbox_fields`). The match maps:
    - FieldKey::ExtraVolumes => caps.supports_arbitrary_volume_paths
    - FieldKey::MountSsh => caps.supports_arbitrary_volume_paths
    - FieldKey::VolumeIgnores => caps.supports_anonymous_volumes
    - _ => true

    Per D-01: filter-at-build-time approach. In `build_sandbox_fields`, after the existing `container_runtime` resolve_value call (line 921-925), add a `let caps = match container_runtime { ... }` block that maps each ContainerRuntimeName variant to its RuntimeBase constant's `.capabilities` field (Docker -> RuntimeBase::DOCKER.capabilities, Podman -> RuntimeBase::PODMAN.capabilities, AppleContainer -> RuntimeBase::APPLE_CONTAINER.capabilities, Sbx -> RuntimeBase::SBX.capabilities).

    Change the final `vec![...]` return to a `let mut fields = vec![...]; fields.retain(|f| is_field_applicable(f.key, &caps)); fields` pattern.

    Per D-02: add a `#[test] fn test_field_visibility_matches_capabilities()` in the existing `#[cfg(test)] mod tests` block. The test:
    1. Defines an array of (ContainerRuntimeName, RuntimeCapabilities) tuples for all four runtimes.
    2. For each runtime, calls `is_field_applicable` for every sandbox FieldKey.
    3. Asserts specific expectations: for Sbx, ExtraVolumes/MountSsh/VolumeIgnores are NOT applicable. For Docker/Podman/AppleContainer, ALL sandbox fields ARE applicable (these runtimes have all capabilities true for volume-related flags).
    4. Additionally, constructs a `Config` and `ProfileConfig` with each runtime set as `container_runtime`, calls `build_sandbox_fields(SettingsScope::Global, &config, &profile)`, and asserts the returned vec excludes ExtraVolumes/MountSsh/VolumeIgnores for Sbx but includes them for Docker.
  </action>
  <verify>
    <automated>cd /Users/gbryer/projects/other/agent-of-empires && cargo test -p agent-of-empires -- field_visibility_matches_capabilities --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - src/tui/settings/fields.rs contains `fn is_field_applicable(key: FieldKey, caps: &RuntimeCapabilities) -> bool`
    - build_sandbox_fields calls `.retain(|f| is_field_applicable(f.key, &caps))` after constructing the vec
    - The retain uses capabilities resolved from the function's `container_runtime` variable, NOT from a global lookup
    - Test `test_field_visibility_matches_capabilities` exists in the in-module tests block
    - Test asserts that for Sbx: ExtraVolumes, MountSsh, VolumeIgnores return false from is_field_applicable
    - Test asserts that for Docker: all sandbox FieldKeys return true from is_field_applicable
    - `cargo test -p agent-of-empires -- field_visibility` exits 0
    - `cargo clippy -- -D warnings` passes with no new warnings
  </acceptance_criteria>
  <done>The settings TUI excludes sbx-incompatible fields when Sbx is the resolved runtime; a cross-product test proves field visibility matches capability declarations for all four runtimes; existing Docker/Podman/AppleContainer behavior is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add sbx runtime selector e2e test and web session integration test</name>
  <files>tests/e2e/sandbox.rs, tests/web_session_create.rs</files>
  <read_first>
    - tests/e2e/sandbox.rs (existing sandbox e2e test pattern)
    - tests/e2e/harness.rs (TuiTestHarness API: new, spawn_tui, send_keys, wait_for, capture_screen, run_cli)
    - tests/e2e/serve.rs (TUI navigation pattern with send_keys and wait_for)
    - tests/e2e/main.rs (module declarations)
    - src/containers/sbx/mod.rs (SbxRuntime::new(), is_available())
  </read_first>
  <action>
    Per D-09 and D-10: Add a new test to tests/e2e/sandbox.rs:

    `#[test] #[serial] #[ignore = "requires sbx daemon"] fn test_sbx_runtime_selector()` that:
    1. Creates a TuiTestHarness with name "sbx_runtime_selector"
    2. Uses run_cli to set the container runtime to Sbx: `h.run_cli(&["config", "set", "sandbox.container_runtime", "sbx"])` (or the equivalent; check existing config CLI for the exact subcommand; if no config set subcommand exists, pre-seed the config.toml in the harness home dir with `container_runtime = "Sbx"` before spawning)
    3. Spawns the TUI via h.spawn_tui()
    4. Navigates to Settings (send_keys "," which opens settings)
    5. Navigates to the Sandbox section
    6. Waits for "Container Runtime" text to appear
    7. Asserts screen contains "Docker Sandboxes" (the Sbx display label)
    8. Verifies the setting persists by checking the config file in the harness home directory

    Per D-08: Create a new file tests/web_session_create.rs with a `#[test] #[ignore = "requires sbx daemon and serve feature"] fn web_session_create_sbx()` that:
    1. Guards with `if !sbx_available() { eprintln!("Skipping: sbx not available"); return; }` where sbx_available checks `SbxRuntime::new().is_available()`
    2. Documents that it would start `aoe serve --no-auth`, create a session via HTTP POST to the API, and confirm the session shows Running status
    3. Since this test requires the serve feature and a running sbx daemon, it serves as a scaffold that verifies the test compiles and is properly gated

    Note: The web test may require `--features serve` to access server types. If the test cannot import server types without the feature flag, add a `#[cfg(feature = "serve")]` gate around the test body and document this in the test's doc comment.

    Do NOT add sbx-specific code to src/server/ or src/cockpit/ (per D-07). The test verifies transparency by confirming existing code paths work with sbx.
  </action>
  <verify>
    <automated>cd /Users/gbryer/projects/other/agent-of-empires && cargo test --test e2e -- sbx_runtime_selector --list 2>&1 | grep -q "sbx_runtime_selector" && cargo test --test web_session_create -- --list 2>&1 | grep -q "web_session_create_sbx" && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - tests/e2e/sandbox.rs contains a function named `test_sbx_runtime_selector` with `#[ignore = "requires sbx daemon"]`
    - tests/web_session_create.rs exists with `#[ignore = "requires sbx daemon and serve feature"]` on the test function
    - Both tests compile without errors (cargo test --test e2e -- --list succeeds; cargo test --test web_session_create -- --list succeeds)
    - No new code added to src/server/ or src/cockpit/
    - `cargo clippy -- -D warnings` passes
    - tests/e2e/main.rs does NOT need modification (sandbox.rs is already declared as a module)
  </acceptance_criteria>
  <done>E2e test for sbx runtime selector TUI and web session integration test are scaffolded, compile, and are properly gated behind #[ignore] for CI. Both test names are discoverable via --list.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Settings TUI input | User selects runtime from a fixed dropdown; no untrusted string input |
| Test harness | Tests run in isolated temp dirs; no production state access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | build_sandbox_fields | accept | Field list is constructed server-side from a fixed enum; no user-controlled input flows into the filter predicate |
| T-06-02 | Information Disclosure | cross-product test | accept | Test only asserts capability values that are already public constants in the binary |
| T-06-03 | Denial of Service | web_session_create test | accept | Test is #[ignore]-gated and only runs in controlled environments |
</threat_model>

<verification>
1. `cargo test -p agent-of-empires -- field_visibility_matches_capabilities` passes
2. `cargo test --test e2e -- --list` includes `sbx_runtime_selector`
3. `cargo test --test web_session_create -- --list` includes `web_session_create_sbx`
4. `cargo clippy -- -D warnings` produces no new warnings
5. `cargo fmt --check` produces no diff
6. No files modified in `src/server/` or `src/cockpit/`
</verification>

<success_criteria>
- Settings TUI with Sbx runtime selected shows 10 sandbox fields (13 minus ExtraVolumes, VolumeIgnores, MountSsh)
- Settings TUI with Docker/Podman/AppleContainer selected shows all 13 sandbox fields
- Cross-product unit test passes asserting the field-capability relationship
- E2e test scaffolded and compilable for sbx runtime selector
- Web integration test scaffolded and compilable
- Zero sbx-specific code in src/server/ or src/cockpit/
</success_criteria>

<output>
After completion, create `.planning/phases/06-settings-tui-cross-machine-integration-and-sleep-wake/06-01-SUMMARY.md`
</output>
