---
phase: 06-settings-tui-cross-machine-integration-and-sleep-wake
plan: 03
type: execute
wave: 3
depends_on: [06-01, 06-02]
files_modified:
  - src/containers/sbx/mod.rs
  - src/containers/sbx/argv.rs
  - src/containers/sbx_kit/spec.yaml
autonomous: true
requirements: [RT-01, INT-01, INT-02, SET-01, SET-02, TEST-04]
gap_closure: true

must_haves:
  truths:
    - "Sbx session creation completes — sandbox transitions from stopped to running before the terminal attaches"
    - "SSH agent forwarding works inside sbx sandboxes — ssh -T git@github.com authenticates using host keys"
    - "start_container calls sbx run and polls for running state before returning Ok"
  artifacts:
    - path: "src/containers/sbx/mod.rs"
      provides: "start_container implementation that calls sbx run"
      contains: "sbx.*run"
    - path: "src/containers/sbx/argv.rs"
      provides: "build_create_args passes SSH_AUTH_SOCK env to sbx create"
      contains: "SSH_AUTH_SOCK"
    - path: "src/containers/sbx_kit/spec.yaml"
      provides: "startup command for SSH agent relay socat bridge"
      contains: "socat"
  key_links:
    - from: "src/session/instance.rs:1250"
      to: "src/containers/sbx/mod.rs::start_container"
      via: "container.start() dispatch"
      pattern: "sbx.*run"
    - from: "src/containers/sbx/mod.rs::start_container"
      to: "src/containers/sbx/mod.rs::is_container_running"
      via: "polling loop after sbx run"
      pattern: "is_container_running"
---

<objective>
Close two UAT gaps from Phase 6 testing: (1) sbx session creation stuck on "Creating..." because start_container is a no-op and (2) SSH agent forwarding broken because the host SSH_AUTH_SOCK is not communicated to the sbx daemon for relay setup.

Purpose: Without these fixes, sbx sessions cannot start at all (blocker) and git SSH operations fail inside sandboxes (major). These are the final blockers before Phase 6 can close.

Output: Working sbx session lifecycle (create -> start -> exec) and functioning SSH agent relay inside sbx sandboxes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-settings-tui-cross-machine-integration-and-sleep-wake/06-UAT.md
@.planning/phases/06-settings-tui-cross-machine-integration-and-sleep-wake/06-01-SUMMARY.md
@.planning/phases/06-settings-tui-cross-machine-integration-and-sleep-wake/06-02-SUMMARY.md

<interfaces>
From src/containers/sbx/mod.rs:
- pub struct SbxRuntime { pub(crate) binary: PathBuf }
- pub fn start_container(&self, _name: &str) -> ContainerResult<()> — currently a no-op
- fn wait_for_ready(&self, name: &str) -> ContainerResult<()> — backoff loop checking sandbox_status().is_ok()
- pub fn is_container_running(&self, name: &str) -> ContainerResult<bool> — checks status == "running"
- pub(crate) fn run(&self, args: &[&str]) -> ContainerResult<Output> — subprocess wrapper

From src/containers/sbx/argv.rs:
- pub fn build_create_args(name, image, kit_path, config) -> Vec<String>

From src/containers/container_interface.rs:
- pub struct ContainerConfig { working_dir, volumes, anonymous_volumes, environment: Vec<EnvEntry>, cpu_limit, memory_limit, port_mappings, agent_name }
- pub enum EnvEntry { Inherit { key, value }, Literal { key, value } }

From src/containers/runtime.rs:
- fn start_container(&self, name: &str) dispatches to sbx.start_container(name)
- DockerContainer::start() calls runtime.start_container(name)

From src/session/instance.rs:1248-1251:
- if container.exists() { container.start()?; return Ok(container); }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement start_container to call sbx run and poll for running state</name>
  <files>src/containers/sbx/mod.rs</files>
  <read_first>
    - src/containers/sbx/mod.rs (full file, especially start_container at line 155, wait_for_ready at line 93, is_container_running at line 251, the run() helper at line 53)
    - src/session/instance.rs (lines 1240-1270, the get_container_for_instance flow that calls container.start())
    - src/containers/runtime.rs (lines 287-296, start_container dispatch)
  </read_first>
  <action>
    Replace the no-op start_container at mod.rs:155-157 with an implementation that:

    1. Spawns `sbx run <name>` as a detached background process using Command::new(self.binary).args(["run", name]).stdin(Stdio::null()).stdout(Stdio::null()).stderr(Stdio::piped()).spawn(). The spawned child starts the sandbox and attaches interactively, but since stdin is null it will either keep the sandbox running or exit. The key insight: `sbx run` transitions the sandbox from "stopped" to "running". We spawn it, then poll for the sandbox to reach "running" state, then drop (kill) the child since the real terminal will attach separately via `sbx exec`.

    2. After spawning, poll is_container_running(name) with exponential backoff delays [200, 400, 800, 1600, 3200, 6400, 12800] ms (reuse the same pattern as wait_for_ready). If is_container_running returns true, kill the spawned child process (child.kill() is fine, the sandbox stays running independently), and return Ok(()).

    3. If after all delays the sandbox is still not running, kill the child process, read its stderr, and return Err(DockerError::CommandFailed) with a message including "sbx sandbox not running after start attempt" and any stderr content.

    4. Add tracing::info at entry: target "containers.sbx", name, "starting sandbox".

    5. Remove the test test_start_container_is_noop (it asserts the old no-op behavior). Replace with test_start_container_calls_run that uses a mock script which: (a) logs args to a file, (b) when invoked with "run" as first arg, outputs a valid sbx ls JSON containing the named sandbox with status "running" when subsequently queried with "ls --json", and (c) exits 0. The test should assert that "run" appears in the logged args and start_container returns Ok.

    Key detail: the mock script must handle both the "run" invocation (from the spawned child) AND the "ls --json" invocations (from is_container_running polling). Use the shell script's $1 arg to dispatch behavior: if $1 == "run", write a flag file and sleep briefly; if $1 == "ls", output JSON with status "running" if the flag file exists, "stopped" otherwise.
  </action>
  <acceptance_criteria>
    - src/containers/sbx/mod.rs start_container method body contains Command::new with args containing "run"
    - start_container spawns a child process with Stdio::null() on stdin
    - After spawn, a polling loop calls is_container_running with backoff delays
    - On success (running == true), child.kill() is called and Ok(()) returned
    - On timeout, Err with message containing "not running after start" is returned
    - tracing::info! with target "containers.sbx" is present at method entry
    - No test named test_start_container_is_noop exists
    - A test test_start_container_calls_run exists that verifies "run" was invoked and Ok is returned
    - cargo test --lib -- sbx::tests::test_start_container passes
    - cargo clippy -- -D warnings passes with no new warnings
  </acceptance_criteria>
  <verify>
    <automated>cargo test --lib -- sbx::tests::test_start_container_calls_run && cargo clippy -- -D warnings 2>&1 | tail -5</automated>
  </verify>
  <done>start_container spawns sbx run, polls for running state, kills child on success, returns Err on timeout. Old no-op test removed, new test passes.</done>
</task>

<task type="auto">
  <name>Task 2: Pass SSH_AUTH_SOCK to sbx create and add startup relay command</name>
  <files>src/containers/sbx/argv.rs, src/containers/sbx_kit/spec.yaml, src/containers/sbx/mod.rs</files>
  <read_first>
    - src/containers/sbx/argv.rs (full file, especially build_create_args starting at line 24)
    - src/containers/sbx_kit/spec.yaml (full file, especially credentials section at line 110-114 and commands.startup at line 11)
    - src/containers/sbx/mod.rs (create_container at line 109-151, especially the environment forwarding loop at line 128-132)
    - src/session/environment.rs (lines 307-335, collect_environment)
    - src/session/container_config.rs (lines 1157-1170, environment assembly)
  </read_first>
  <action>
    The SSH agent forwarding issue has two parts: (A) sbx needs to know the host SSH_AUTH_SOCK path so it can set up the relay socket, and (B) the kit must start a socat relay inside the sandbox bridging the sbx-provided relay socket to the well-known /run/ssh-agent.sock path.

    Part A: In src/containers/sbx/mod.rs create_container, after materializing the kit and before calling Command::new, check if the host has SSH_AUTH_SOCK set via std::env::var("SSH_AUTH_SOCK"). If present, add it to the Command environment via cmd.env("SSH_AUTH_SOCK", &host_sock_path). This tells the sbx daemon where to find the host agent socket so it can create a relay. The spec.yaml credentials.sources.ssh.env already declares SSH_AUTH_SOCK as a credential source, which tells sbx to relay it; but the daemon needs the actual path from the calling process's environment.

    Additionally, in build_create_args (argv.rs), add a new parameter ssh_auth_sock: Option<&str>. When Some(path), emit -e SSH_AUTH_SOCK=<path> BEFORE the sandbox name/paths in the args vec (after the --template and resource args but before positional paths). Update all call sites of build_create_args (there are two: the one in create_container at mod.rs:122, and the one in runtime.rs build_create_args dispatch). For the runtime.rs call site, pass None (that code path is for the string-formatting case, not the actual create). For the create_container call in mod.rs, pass the resolved SSH_AUTH_SOCK value.

    Part B: In src/containers/sbx_kit/spec.yaml, add a startup command that starts the socat relay if /run/ssh-agent.sock exists but is not already functioning. The startup command goes in the commands.startup array (after the existing agent binary check). The command should be:
    ["sh", "-c", "[ -S /run/ssh-agent.sock ] || exit 0; ssh-add -l >/dev/null 2>&1 && exit 0; exit 0"]

    Actually, the correct approach for sbx SSH relay: the spec.yaml credentials.sources.ssh.env declaration tells sbx to make SSH_AUTH_SOCK available inside the sandbox. The relay is handled by the sbx daemon itself (it creates the socket at /run/ssh-agent.sock). The issue is that the sbx daemon needs the host-side SSH_AUTH_SOCK path in its process environment at create time to set up the relay. So Part A (passing the env var) is the real fix. Part B should instead verify the relay is operational with a startup check that sets SSH_AUTH_SOCK=/run/ssh-agent.sock if not already set, since the agent binary needs this env var pointing to the relay socket.

    Update spec.yaml: add a startup entry before the agent-binary-check: ["sh", "-c", "export SSH_AUTH_SOCK=/run/ssh-agent.sock; [ -S \"$SSH_AUTH_SOCK\" ] && ssh-add -l >/dev/null 2>&1 || true"]

    This ensures that (1) the env var is set correctly inside the sandbox and (2) the relay is probed at startup (logging any issue without failing).

    Update the existing argv tests: add a test create_args_with_ssh_auth_sock that passes ssh_auth_sock=Some("/tmp/agent.sock") and asserts -e SSH_AUTH_SOCK=/tmp/agent.sock appears in the output. Add a test create_args_without_ssh_auth_sock that passes None and asserts no SSH_AUTH_SOCK in output.
  </action>
  <acceptance_criteria>
    - argv.rs build_create_args signature includes ssh_auth_sock: Option<&str> parameter
    - When ssh_auth_sock is Some(path), the output vec contains "-e" followed by "SSH_AUTH_SOCK=<path>"
    - When ssh_auth_sock is None, no SSH_AUTH_SOCK appears in the output vec
    - mod.rs create_container reads std::env::var("SSH_AUTH_SOCK") and passes it to build_create_args
    - mod.rs create_container also calls cmd.env("SSH_AUTH_SOCK", path) on the Command before .output()
    - spec.yaml commands.startup contains a command with "SSH_AUTH_SOCK=/run/ssh-agent.sock"
    - Test create_args_with_ssh_auth_sock exists and passes
    - Test create_args_without_ssh_auth_sock exists and passes
    - All existing argv tests still pass (build_create_args call sites updated with None for ssh_auth_sock)
    - cargo test --lib -- sbx::argv::tests passes
    - cargo test --lib -- sbx::kit::tests passes (kit linter accepts updated spec.yaml)
    - cargo clippy -- -D warnings passes
  </acceptance_criteria>
  <verify>
    <automated>cargo test --lib -- sbx::argv::tests && cargo test --lib -- sbx::kit::tests && cargo clippy -- -D warnings 2>&1 | tail -5</automated>
  </verify>
  <done>Host SSH_AUTH_SOCK path is forwarded to sbx create via both process env and -e flag in argv. Kit startup probes the relay socket. Tests verify both presence and absence of the SSH env arg.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host -> sbx daemon | Commands spawned to sbx CLI cross into the sbx daemon process |
| host env -> sandbox | SSH_AUTH_SOCK path from host is passed to sandbox create command |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | I (Information Disclosure) | SSH_AUTH_SOCK path leaked in process argv | accept | Path is not secret (visible in /proc on Linux anyway); sbx exec already exposes env vars in argv. No PII. |
| T-06-03-02 | D (Denial of Service) | start_container spawns sbx run that hangs indefinitely | mitigate | Polling loop has 25s max budget; child.kill() on timeout ensures no zombie process. |
| T-06-03-03 | T (Tampering) | Malicious sbx binary injected via PATH | accept | Same trust model as Docker CLI; user controls their PATH. Not in scope for aoe to validate. |
</threat_model>

<verification>
1. cargo test --lib -- sbx::tests passes (start_container test)
2. cargo test --lib -- sbx::argv::tests passes (SSH env arg tests)
3. cargo test --lib -- sbx::kit::tests passes (spec.yaml lint)
4. cargo clippy -- -D warnings produces no warnings
5. cargo fmt --check produces no diff
6. Manual: create an sbx session in aoe TUI, verify it transitions to Running
7. Manual: inside the running sbx sandbox, run ssh -T git@github.com and verify authentication succeeds
</verification>

<success_criteria>
- sbx sessions created through aoe transition from "Creating..." to Running within 30 seconds
- SSH agent forwarding works end-to-end inside sbx sandboxes (git SSH operations authenticate)
- No regressions in existing sbx tests (argv builders, kit linter, capability matrix)
- No clippy warnings, no fmt diff
</success_criteria>

<output>
After completion, create `.planning/phases/06-settings-tui-cross-machine-integration-and-sleep-wake/06-03-SUMMARY.md`
</output>
