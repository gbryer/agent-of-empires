---
phase: 07-user-documentation-and-cli-reference-sync
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - docs/guides/sandbox.md
  - website/scripts/sync-docs.mjs
  - website/src/data/docsNav.ts
  - docs/cli/reference.md
autonomous: true
requirements:
  - DOC-01
  - DOC-02

must_haves:
  truths:
    - "docs/guides/sandbox.md contains a callout line linking to sbx.md"
    - "website/scripts/sync-docs.mjs PAGES array includes an entry for docs/guides/sbx.md"
    - "website/scripts/sync-docs.mjs URL_MAP includes docs/guides/sbx.md -> /guides/sbx/"
    - "website/src/data/docsNav.ts Guides section includes a Docker Sandboxes (sbx) nav item with href /guides/sbx/"
    - "cargo xtask gen-docs produces zero diff against committed docs/cli/reference.md"
  artifacts:
    - path: "docs/guides/sandbox.md"
      provides: "Callout to sbx guide"
      contains: "sbx.md"
    - path: "website/scripts/sync-docs.mjs"
      provides: "PAGES + URL_MAP entries for sbx guide"
      contains: "docs/guides/sbx.md"
    - path: "website/src/data/docsNav.ts"
      provides: "Nav entry for sbx guide"
      contains: "/guides/sbx/"
  key_links:
    - from: "website/scripts/sync-docs.mjs"
      to: "docs/guides/sbx.md"
      via: "PAGES source field"
      pattern: "source.*docs/guides/sbx\\.md"
    - from: "website/scripts/sync-docs.mjs"
      to: "website/src/data/docsNav.ts"
      via: "nav validation at lines 308-323"
      pattern: "navHrefs\\.has"
    - from: "docs/guides/sandbox.md"
      to: "docs/guides/sbx.md"
      via: "relative markdown link in callout"
      pattern: "\\[.*sbx.*\\]\\(sbx\\.md\\)"
---

<objective>
Wire the sbx guide into the website, add a callout in sandbox.md, and ensure docs/cli/reference.md is up to date via cargo xtask gen-docs.

Purpose: Completes DOC-01 (website wiring and sandbox.md callout) and DOC-02 (CLI reference sync). The new guide becomes discoverable on agent-of-empires.com and from the existing Docker sandbox guide.

Output: Modified sandbox.md, sync-docs.mjs, docsNav.ts, and verified-current docs/cli/reference.md.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-user-documentation-and-cli-reference-sync/07-CONTEXT.md
@.planning/phases/07-user-documentation-and-cli-reference-sync/07-RESEARCH.md
@.planning/phases/07-user-documentation-and-cli-reference-sync/07-01-SUMMARY.md

<interfaces>
<!-- sync-docs.mjs PAGES entry shape (from existing sandbox.md entry) -->
{
  source: "docs/guides/sandbox.md",
  dest: "guides/sandbox.md",
  title: "Docker Sandbox: Quick Reference",
  description: "Run AI coding agents in isolated Docker containers with Agent of Empires.",
},

<!-- sync-docs.mjs URL_MAP entry shape -->
"docs/guides/sandbox.md": "/guides/sandbox/",

<!-- docsNav.ts nav item shape (Guides section) -->
{ title: "Docker Sandbox", href: "/guides/sandbox/" },

<!-- sandbox.md callout pattern (lines 7-9) -->
> **Linux users:** AoE also supports [Podman](podman.md) as a daemonless, rootless-friendly alternative to Docker.
>
> **macOS users:** AoE also supports [Apple Containers](apple-containers.md) as a native alternative to Docker Desktop.

<!-- sync-docs.mjs nav validation (lines 308-323) -->
For dest: "guides/sbx.md", the expected href is "/guides/sbx/" (computed as "/" + dest.replace(/\.md$/, "/"))
sync-docs.mjs exits with code 1 if any PAGES entry lacks a matching docsNav.ts href.

<!-- Pitfall 4: gen-docs likely produces no diff -->
ContainerRuntimeName::Sbx is NOT a clap ValueEnum. Runtime is set via config/TUI, not CLI args.
cargo xtask gen-docs likely produces no diff from sbx addition. Per D-09: if no diff, commit nothing for this part.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sbx callout to sandbox.md and wire sbx guide into website</name>
  <files>docs/guides/sandbox.md, website/scripts/sync-docs.mjs, website/src/data/docsNav.ts</files>
  <read_first>
    docs/guides/sandbox.md
    website/scripts/sync-docs.mjs
    website/src/data/docsNav.ts
    .planning/phases/07-user-documentation-and-cli-reference-sync/07-CONTEXT.md
    .planning/phases/07-user-documentation-and-cli-reference-sync/07-RESEARCH.md
  </read_first>
  <action>
Three file edits, all following established patterns:

**1. docs/guides/sandbox.md** per D-07: Add a one-line callout immediately after the existing Podman and Apple Containers callouts (after line 9). Follow the exact blockquote pattern already used at lines 7-9. The new callout should read:
> **Looking for microVM-grade isolation?** AoE also supports [Docker Sandboxes (sbx)](sbx.md) for microVM-based sandboxing via Docker Desktop.

Add it with a blank `>` line separator before it (matching the pattern of a blank blockquote line between the existing callouts).

**2. website/scripts/sync-docs.mjs** per D-03:
- Add a PAGES entry in the Guides section (after the existing sandbox.md entry around line 42). The entry shape: source is "docs/guides/sbx.md", dest is "guides/sbx.md", title is "Docker Sandboxes (sbx)" per D-02, description is "Set up Docker Sandboxes (sbx) as a microVM-based sandbox runtime for Agent of Empires."
- Add a URL_MAP entry: key "docs/guides/sbx.md", value "/guides/sbx/" per D-03.

**3. website/src/data/docsNav.ts** per D-03: Add a nav item in the Guides section items array. Place it immediately after the "Docker Sandbox" entry (currently the first item in Guides at line 28). The entry: title is "Docker Sandboxes (sbx)", href is "/guides/sbx/". The href MUST have a trailing slash and match exactly what sync-docs.mjs computes: "/" + "guides/sbx.md".replace(/\.md$/, "/") = "/guides/sbx/".

CRITICAL: The docsNav.ts href must be exactly "/guides/sbx/" or sync-docs.mjs will fail validation (Pitfall 2 from RESEARCH.md).
  </action>
  <verify>
    <automated>grep -q "sbx.md" docs/guides/sandbox.md &amp;&amp; grep -q '"docs/guides/sbx.md"' website/scripts/sync-docs.mjs &amp;&amp; grep -c "docs/guides/sbx.md" website/scripts/sync-docs.mjs | grep -q "^2$" &amp;&amp; grep -q '"/guides/sbx/"' website/src/data/docsNav.ts &amp;&amp; echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - docs/guides/sandbox.md contains a blockquote callout with "[Docker Sandboxes (sbx)](sbx.md)" linking to the new guide
    - website/scripts/sync-docs.mjs PAGES array contains an entry with source: "docs/guides/sbx.md" and dest: "guides/sbx.md"
    - website/scripts/sync-docs.mjs URL_MAP contains "docs/guides/sbx.md": "/guides/sbx/"
    - website/src/data/docsNav.ts Guides items array contains { title: "Docker Sandboxes (sbx)", href: "/guides/sbx/" }
    - The docsNav.ts entry appears immediately after the Docker Sandbox entry
    - "docs/guides/sbx.md" appears exactly twice in sync-docs.mjs (once in PAGES, once in URL_MAP)
  </acceptance_criteria>
  <done>The sbx guide is wired into the website (PAGES, URL_MAP, nav) and sandbox.md has a callout linking to it.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate CLI reference and verify CI readiness</name>
  <files>docs/cli/reference.md</files>
  <read_first>
    docs/cli/reference.md
    .planning/phases/07-user-documentation-and-cli-reference-sync/07-CONTEXT.md
  </read_first>
  <action>
Per D-09 and DOC-02: Run `cargo xtask gen-docs` to regenerate docs/cli/reference.md from current clap definitions.

After running, check `git diff docs/cli/reference.md`. Per RESEARCH.md Pitfall 4, ContainerRuntimeName::Sbx is NOT a clap ValueEnum, so gen-docs will likely produce no diff. If no diff exists, docs/cli/reference.md is already current and nothing needs to be committed for this file.

If gen-docs DOES produce a diff (e.g., Phases 5/6 added or changed clap help text), stage and commit the regenerated file.

Either way, verify the CI check passes: `cargo xtask gen-docs` followed by `git diff --exit-code docs/cli/reference.md` must exit 0 (no diff between generated and committed).
  </action>
  <verify>
    <automated>cargo xtask gen-docs &amp;&amp; git diff --exit-code docs/cli/reference.md &amp;&amp; echo "PASS: CLI reference is up to date"</automated>
  </verify>
  <acceptance_criteria>
    - `cargo xtask gen-docs` completes without error
    - `git diff --exit-code docs/cli/reference.md` exits 0 (committed file matches generated output)
    - If gen-docs produced a diff, the regenerated file is committed; if no diff, no commit for this file
  </acceptance_criteria>
  <done>docs/cli/reference.md is verified current against clap definitions. The CI gen-docs check will pass with zero diff.</done>
</task>

</tasks>

<verification>
- sandbox.md callout links to sbx.md
- sync-docs.mjs has both PAGES and URL_MAP entries for docs/guides/sbx.md
- docsNav.ts has nav entry with href "/guides/sbx/"
- cargo xtask gen-docs && git diff --exit-code docs/cli/reference.md exits 0
</verification>

<success_criteria>
- All three website wiring files are updated per the AGENTS.md 4-step recipe
- sandbox.md has the callout per D-07
- CLI reference is verified current per DOC-02
- sync-docs.mjs validation would pass (nav href matches computed URL)
</success_criteria>

<output>
After completion, create `.planning/phases/07-user-documentation-and-cli-reference-sync/07-02-SUMMARY.md`
</output>
