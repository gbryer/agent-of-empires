---
phase: 04-embedded-sbx-kit-and-materialization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/containers/sbx_kit/spec.yaml
  - src/containers/sbx_kit/hook-shim.sh
  - src/containers/sbx_kit/gitignore-aoe-hooks
  - src/containers/sbx_kit/settings-claude.json
  - src/containers/sbx_kit/settings-gemini.json
  - src/containers/sbx_kit/settings-cursor.json
  - src/containers/sbx_kit/settings-qwen.json
  - src/containers/sbx/kit.rs
  - src/containers/sbx/mod.rs
autonomous: true
requirements:
  - KIT-01
  - KIT-02
  - KIT-03
  - KIT-04
  - KIT-05

must_haves:
  truths:
    - "Embedded kit bytes (spec.yaml + hook-shim.sh + gitignore-aoe-hooks + per-agent settings.json files) are compiled into the aoe binary via include_str! (D-01, D-04, D-05, D-06)"
    - "KitMaterializer::ensure(agent) materializes the per-agent dir lazily on first call and is a no-op on subsequent calls with the same (aoe_version, spec_hash, agent) (D-08, D-11, D-12, D-13)"
    - "Mutating one byte in any embedded kit file changes the resolved cache dir name without affecting the prior dir on disk (D-13, KIT-05)"
    - "Stale top-level cache dirs older than 30 days are removed in a fire-and-forget tokio::task::spawn_blocking task triggered ONLY on cache miss (D-14, D-15)"
    - "The materialized kit's commands.install body comes from src/agents.rs::install_hint(agent) for the v1 subset (claude, codex, opencode, gemini, droid, pi, qwen, kiro, hermes); agents outside the subset get a fail-loud install body (D-09, D-10)"
    - "spec.yaml declares schemaVersion: \"1\", kind: agent, name, commands.install (array), commands.startup (array), initFiles for the four hook-supporting agents (claude/gemini/cursor/qwen), and environment.proxyManaged with the provider env vars (D-06, D-16)"
    - "spec.yaml declares a credentials.sources block requesting ssh-agent forwarding (honors locked D-05 with [ASSUMED] schema, paired with manual-verification entry in VALIDATION.md)"
    - "The in-VM hook shim writes status to ${WORKDIR}/.aoe-hooks/$AOE_INSTANCE_ID/status; ${WORKDIR} appears ONLY in initFiles[*].content, NEVER in initFiles[*].path (D-01, RESEARCH Pitfall 5)"
    - "Concurrent ensure() racers all succeed; the winning dir contents are byte-equal to any losing tmp dir (D-12, D-13)"
    - "v1 spec.yaml does NOT declare gitconfig identity, agent-config dirs beyond hook-shim settings.json, GCP creds, or home-seed files; per D-07 these are explicitly deferred to Phase 5/6 (Phase 3 already silently dropped them under sbx)"
  artifacts:
    - path: "src/containers/sbx_kit/spec.yaml"
      provides: "Single embedded kit spec; schemaVersion 1; kind agent; install/startup commands; initFiles for hook-supporting agents; environment.proxyManaged; credentials.sources ssh-agent block"
      contains: "schemaVersion: \"1\""
    - path: "src/containers/sbx_kit/hook-shim.sh"
      provides: "Reusable shell snippet for the in-VM status writer; reads $AOE_INSTANCE_ID + ${WORKDIR}"
      contains: "AOE_INSTANCE_ID"
    - path: "src/containers/sbx_kit/gitignore-aoe-hooks"
      provides: "Workspace-relative .gitignore content for .aoe-hooks/"
      contains: ".aoe-hooks/"
    - path: "src/containers/sbx_kit/settings-claude.json"
      provides: "Claude Code in-VM settings.json with hooks pointing at hook-shim.sh"
    - path: "src/containers/sbx_kit/settings-gemini.json"
      provides: "Gemini in-VM settings.json with hooks"
    - path: "src/containers/sbx_kit/settings-cursor.json"
      provides: "Cursor CLI in-VM settings.json with hooks"
    - path: "src/containers/sbx_kit/settings-qwen.json"
      provides: "Qwen Code in-VM settings.json with hooks"
    - path: "src/containers/sbx/kit.rs"
      provides: "KitMaterializer::ensure, cache_dir, gc_stale_kit_dirs, embedded_kit_bytes; in-module #[cfg(test)] tests"
      exports:
        - "ensure"
        - "cache_dir"
    - path: "src/containers/sbx/mod.rs"
      provides: "Registers `pub mod kit;` so kit.rs joins argv.rs as a sibling sub-module"
  key_links:
    - from: "src/containers/sbx/kit.rs"
      to: "src/containers/sbx_kit/spec.yaml"
      via: "include_str! at module-const declaration"
      pattern: "include_str!\\(\"\\.\\./sbx_kit/spec\\.yaml\"\\)"
    - from: "src/containers/sbx/kit.rs"
      to: "src/agents.rs::install_hint"
      via: "crate::agents::install_hint(agent_name) at install.sh body composition"
      pattern: "crate::agents::install_hint"
    - from: "src/containers/sbx/kit.rs"
      to: "src/session::get_app_dir"
      via: "Cache root resolution at cache_dir() entry"
      pattern: "crate::session::get_app_dir"
    - from: "src/containers/sbx/kit.rs"
      to: "tokio::task::spawn_blocking"
      via: "Fire-and-forget GC sweep on cache miss"
      pattern: "tokio::task::spawn_blocking"
---

<objective>
Author and embed the single aoe-authored sbx kit (spec.yaml, hook-shim.sh, gitignore-aoe-hooks, per-agent settings.json files for the four hook-supporting agents). Ship the KitMaterializer with content-addressed lazy cache, atomic-rename concurrency, and 30-day stale-dir GC.

Purpose: KIT-01..KIT-03 + KIT-05 are entirely owned by this plan. KIT-04 is partially closed here (the in-VM shim content and the gitignore drop content); the host-side dispatch and workspace gitignore-merge guarantee land in 04-02. KIT-02 (`sbx create shell --kit <materialized> --template <image>` invocation shape) is verified through Phase 2's existing argv tests plus a new test that feeds a `KitMaterializer::ensure` output into `build_create_args`.

Output: New `src/containers/sbx_kit/` directory with seven asset files. New `src/containers/sbx/kit.rs` module. Single-line edit to `src/containers/sbx/mod.rs` registering the new sub-module.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-embedded-sbx-kit-and-materialization/04-CONTEXT.md
@.planning/phases/04-embedded-sbx-kit-and-materialization/04-RESEARCH.md
@.planning/phases/04-embedded-sbx-kit-and-materialization/04-PATTERNS.md
@.planning/phases/04-embedded-sbx-kit-and-materialization/04-VALIDATION.md
@.planning/sbx-cli-reference.md

@src/agents.rs
@src/containers/sbx/mod.rs
@src/containers/sbx/argv.rs
@src/hooks/mod.rs
@src/logging.rs
@src/session/mod.rs

<interfaces>
<!-- Extracted from codebase. Executor should consume these directly. -->

From src/agents.rs (line 477):
```rust
pub fn install_hint(name: &str) -> Option<&'static str>;
pub fn get_agent(name: &str) -> Option<&'static AgentDef>;
```

`AgentDef::install_hint: &'static str` (line 103), non-empty per src/agents.rs:634 invariant.

Hook-supporting agents (verified via `hook_config: Some(...)` at lines 178, 253, 293, 424):
- claude: `settings_rel_path = ".claude/settings.json"`, events = `CLAUDE_CURSOR_HOOK_EVENTS`
- gemini: `settings_rel_path = ".gemini/settings.json"`
- cursor: `settings_rel_path = ".cursor/settings.json"`, events = `CLAUDE_CURSOR_HOOK_EVENTS`
- qwen:   `settings_rel_path = ".qwen/settings.json"`

v1 install_hint targets per CONTEXT.md D-10 + RESEARCH Open Q3 recommendation:
- npm install -g (claude, codex, gemini, droid, pi, qwen)
- curl|bash (opencode, kiro, hermes)
- pip install (vibe; Python may not be in template image so ship best-effort)
- fail-loud (cursor with "see https://docs.cursor.com/cli" since there is no headless install; copilot, same; settl, Homebrew Cask host-only)

From src/containers/sbx/argv.rs (Phase 2, line 24):
```rust
pub fn build_create_args(
    name: &str,
    image: &str,
    kit_path: Option<&Path>,
    config: &ContainerConfig,
) -> Vec<String>;
```
Plan 04-01 feeds a `KitMaterializer::ensure(agent)?` output into `kit_path` via a new in-module test.

From src/session/mod.rs (line 73):
```rust
pub fn get_app_dir() -> Result<PathBuf>;
```
Already handles per-platform path + `cfg!(debug_assertions)` namespace + `create_dir_all`. Root the cache here.

From src/logging.rs:447-468 (atomic-rename precedent):
- Write tmp, rename, on error fall through. For directory rename, ErrorKind is not reliable; use post-rename `target.exists()` instead (RESEARCH Pitfall 1).
- `#[cfg(unix)]` block at lines 461-465 sets file mode via `std::os::unix::fs::PermissionsExt`; this is a file-permission flag and is the in-tree precedent for the chmod 0o755 used on install.sh below.

From Cargo.toml (already in deps, no additions):
- `sha2 = "0.11"` (line 95), `sha2::{Digest, Sha256}`
- `tokio = "1.50" features = ["full"]` (line 29), `tokio::task::spawn_blocking`
- `tracing = "0.1"` (line 63), `tracing::info!` for GC log
- `anyhow = "1.0"` (line 48), `anyhow::Result<PathBuf>` return shape
- `tempfile = "3.14"` (line 104, dev-dep), `tempfile::TempDir` for tests

Environment intrinsics:
- `env!("CARGO_PKG_VERSION")`, version string for cache key
- `std::process::id()`, pid component of tmp dir suffix
- `std::time::{SystemTime, UNIX_EPOCH}`, nanos component of tmp dir suffix

Existing sbx module shape (src/containers/sbx/mod.rs):
```rust
pub mod argv;
pub(crate) struct SbxRuntime { pub(crate) binary: PathBuf }
impl SbxRuntime { ... }
```
Plan 04-01 adds `pub mod kit;` next to `pub mod argv;`. Nothing else in mod.rs changes.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author embedded kit asset files under src/containers/sbx_kit/</name>
  <files>
    src/containers/sbx_kit/spec.yaml,
    src/containers/sbx_kit/hook-shim.sh,
    src/containers/sbx_kit/gitignore-aoe-hooks,
    src/containers/sbx_kit/settings-claude.json,
    src/containers/sbx_kit/settings-gemini.json,
    src/containers/sbx_kit/settings-cursor.json,
    src/containers/sbx_kit/settings-qwen.json
  </files>
  <read_first>
    .planning/phases/04-embedded-sbx-kit-and-materialization/04-CONTEXT.md,
    .planning/phases/04-embedded-sbx-kit-and-materialization/04-RESEARCH.md,
    .planning/phases/04-embedded-sbx-kit-and-materialization/04-PATTERNS.md,
    .planning/phases/04-embedded-sbx-kit-and-materialization/04-VALIDATION.md,
    .planning/sbx-cli-reference.md,
    src/hooks/mod.rs,
    src/agents.rs
  </read_first>
  <action>
    Create directory `src/containers/sbx_kit/` and the seven asset files described below. None of these files are compiled directly; `src/containers/sbx/kit.rs` (Task 2) embeds them via `include_str!`. Filenames are exactly as listed in `files_modified`.

    File: `spec.yaml` (D-05, D-06, D-16). Top-level keys, in order:
    - `schemaVersion: "1"` (string, not int; RESEARCH `schemaVersion: "1"` is the literal under test).
    - `kind: agent`.
    - `name: aoe-agent`.
    - `commands.install`, single array entry per D-09: `command: ["sh","-c","sh /sbx-kit/install.sh"]` (the materialized `install.sh` is written into the per-agent dir; sbx mounts the `--kit` dir at `/sbx-kit/` inside the VM per sbx kit docs). Add a second `commands.install` entry that idempotently writes the `.aoe-hooks/` line into `${WORKDIR}/.gitignore` using `grep -qsE '^\.aoe-hooks/?$' "${WORKDIR}/.gitignore" || printf '\n.aoe-hooks/\n' >> "${WORKDIR}/.gitignore"` (RESEARCH Pitfall 3; idempotency guard satisfies the check-kit lint).
    - `commands.startup`, single entry asserting the agent binary is on PATH via an idempotency-guarded check: `command: ["sh","-c","command -v claude >/dev/null 2>&1 || command -v codex >/dev/null 2>&1 || command -v opencode >/dev/null 2>&1 || command -v gemini >/dev/null 2>&1 || command -v cursor >/dev/null 2>&1 || command -v droid >/dev/null 2>&1 || command -v pi >/dev/null 2>&1 || command -v qwen >/dev/null 2>&1 || command -v kiro >/dev/null 2>&1 || command -v hermes >/dev/null 2>&1 || (printf 'aoe-agent: no known agent binary on PATH\\n' >&2; exit 1)"]`. The `command -v ... ||` shape satisfies the install-verb idempotency-guard lint introduced in 04-02.
    - `initFiles`, one entry per hook-supporting agent (claude, gemini, cursor, qwen). Each entry has `path` set to the in-VM static absolute path `/home/agent/<rel>` where `<rel>` matches `settings_rel_path` from src/agents.rs (e.g., `.claude/settings.json`), `mode: "0644"`, and `content:` containing the settings.json body (see below). `${WORKDIR}` MUST appear ONLY in `content:`, never in `path:` (RESEARCH Pitfall 5 / A2).
    - `environment.proxyManaged`, array with: `ANTHROPIC_API_KEY`, `OPENAI_API_KEY`, `GEMINI_API_KEY`, `GOOGLE_API_KEY`, `MISTRAL_API_KEY`, `QWEN_API_KEY`, `DEEPSEEK_API_KEY` (D-06). These are declarative; the host sbx proxy injects the real secret per `sbx secret set -g <service>` (Phase 7 docs).
    - DO NOT declare `network.allowedDomains` / `deniedDomains` (RESEARCH Pitfall 4, CONTEXT `<deferred>`).
    - DECLARE `credentials.sources` block requesting ssh-agent forwarding (D-05, restoring SSH parity Phase 3 dropped). The schema for `credentials.sources.<service>.agent: ssh-agent` is [ASSUMED] per RESEARCH A1 (not publicly documented). Use the best-effort schema below and pair with the manual-verification row in VALIDATION.md so a real sbx install can confirm or correct the shape at phase exit. Emit immediately after `environment:` in spec.yaml:

      ```yaml
      # [ASSUMED schema, confirm against a live sbx install before phase exit; see VALIDATION.md row "credentials.sources ssh-agent schema"]
      credentials:
        sources:
          ssh:
            agent: ssh-agent
      ```

      The inline YAML comment is REQUIRED so the next reader (Phase 5 wiring or a contributor reviewing the kit) sees the open assumption inline and knows to validate before relying on it.

    File: `hook-shim.sh`, reference body for the in-VM status-writer command. Single line of shell that mirrors the existing `src/hooks/mod.rs::hook_command` shape but writes to the workspace path: `[ -n "$AOE_INSTANCE_ID" ] || exit 0; mkdir -p "${WORKDIR}/.aoe-hooks/$AOE_INSTANCE_ID" && printf %s "$1" > "${WORKDIR}/.aoe-hooks/$AOE_INSTANCE_ID/status"`. Embedded by `kit.rs` so a future change to the shim shape is a single-file edit. The settings.json `content:` blocks below inline a substituted form (sbx kit format does not source separate files).

    Files: `settings-claude.json`, `settings-gemini.json`, `settings-cursor.json`, `settings-qwen.json`. Each is a JSON object of shape `{"hooks": { <Event>: [ {"hooks": [ {"type": "command", "command": "sh -c '<inline shim>'"} ]} ] }}`. The inline shim is the hook-shim.sh body with `$1` replaced by the literal status string (`running`/`waiting`/`idle`). Events per agent:
    - claude/cursor: `PreToolUse`->running, `UserPromptSubmit`->running, `Stop`->idle, `Notification` (matcher `permission_prompt|elicitation_dialog`)->waiting, `ElicitationResult`->running.
    - gemini: events from src/agents.rs lines 254..; the executor reads `src/agents.rs::get_agent("gemini").hook_config.events` and emits one matcher group per `HookEvent { name, matcher, status: Some(_) }`. Events with `status: None` are skipped (mirrors `build_aoe_hooks` at src/hooks/mod.rs:41).
    - qwen: same approach against `QWEN_HOOK_EVENTS`.
    Each settings.json must use `${WORKDIR}` ONLY inside the `command:` string (so sbx substitutes at sandbox start).

    File: `gitignore-aoe-hooks`. Exact contents: `.aoe-hooks/\n` (one line, trailing newline). Referenced inside spec.yaml's `commands.install` second entry (which appends to the workspace `.gitignore` rather than dropping a separate file, per RESEARCH Pitfall 5; `${WORKDIR}` in initFiles[*].path is undocumented). Used as a string constant in kit.rs tests asserting the spec.yaml install hook contains the expected line.

    Per AGENTS.md: no em-dashes / `--` separators inside the YAML strings. Do not add comments to the YAML except the single inline `[ASSUMED schema ...]` comment above `credentials:` (other comments are stripped by the YAML parser anyway and provide no value).
  </action>
  <verify>
    <automated>cargo build --lib 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/containers/sbx_kit/spec.yaml` exists with first line `schemaVersion: "1"` (string quoted).
    - `grep -c 'kind: agent' src/containers/sbx_kit/spec.yaml` returns at least 1.
    - `grep -c 'environment:' src/containers/sbx_kit/spec.yaml` returns 1; `grep -c 'proxyManaged' src/containers/sbx_kit/spec.yaml` returns 1.
    - `grep -c 'allowedDomains\|deniedDomains' src/containers/sbx_kit/spec.yaml` returns 0.
    - `grep -c '^credentials:' src/containers/sbx_kit/spec.yaml` returns 1 (D-05 honored with [ASSUMED] schema).
    - `grep -c 'ssh-agent' src/containers/sbx_kit/spec.yaml` returns at least 1 (the assumed schema's literal value).
    - `grep -c 'ASSUMED schema' src/containers/sbx_kit/spec.yaml` returns at least 1 (inline comment flagging the assumption for the next reader; VALIDATION.md pairs this with the manual smoke-test row).
    - `python3 -c 'import yaml, sys; d=yaml.safe_load(open("src/containers/sbx_kit/spec.yaml")); assert d["schemaVersion"]=="1"; assert d["kind"]=="agent"; assert d["commands"]["install"]; assert d["commands"]["startup"]; assert d["initFiles"]; assert d["environment"]["proxyManaged"]; assert d["credentials"]["sources"]'` exits 0.
    - `python3 -c 'import yaml, sys; d=yaml.safe_load(open("src/containers/sbx_kit/spec.yaml")); [sys.exit(1) for f in d["initFiles"] if "${WORKDIR}" in f["path"]]; print("ok")'` prints `ok`.
    - `python3 -c 'import yaml, sys; d=yaml.safe_load(open("src/containers/sbx_kit/spec.yaml")); assert any("${WORKDIR}" in f["content"] for f in d["initFiles"])'` exits 0.
    - `src/containers/sbx_kit/hook-shim.sh` exists; `grep -c 'AOE_INSTANCE_ID' src/containers/sbx_kit/hook-shim.sh` returns at least 1; `grep -c '\${WORKDIR}' src/containers/sbx_kit/hook-shim.sh` returns at least 1.
    - `src/containers/sbx_kit/gitignore-aoe-hooks` exists and contains literal `.aoe-hooks/`.
    - Each of `settings-claude.json`, `settings-gemini.json`, `settings-cursor.json`, `settings-qwen.json` exists and parses as valid JSON (`python3 -c 'import json; json.load(open("src/containers/sbx_kit/settings-claude.json"))'` exits 0; same for the others).
    - `grep -vE '^[[:space:]]*#' src/containers/sbx_kit/spec.yaml | grep -nE '— | --[^-]' | grep -v 'sh -c' | wc -l` returns 0 (no em-dashes or `--` separators in non-comment YAML strings; `sh -c` is the only legitimate `--` substring expected; the `[ASSUMED schema...]` comment is excluded because it is a comment).
    - `cargo build --lib` exits 0 (assets are not compiled at this task but the directory must coexist with the existing build).
  </acceptance_criteria>
  <done>
    All seven kit asset files exist under `src/containers/sbx_kit/`. `spec.yaml` parses, declares schemaVersion 1 / kind agent / commands.install / commands.startup / initFiles for four hook-supporting agents / environment.proxyManaged / credentials.sources with ssh-agent forwarding ([ASSUMED] schema commented inline). Omits `network.allowedDomains`. No `${WORKDIR}` in any `initFiles[*].path`. `cargo build --lib` is green. Project's no-em-dash rule respected.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2a: Implement KitMaterializer::ensure + cache_dir + atomic-rename in src/containers/sbx/kit.rs (no GC)</name>
  <files>
    src/containers/sbx/kit.rs,
    src/containers/sbx/mod.rs
  </files>
  <read_first>
    src/containers/sbx/mod.rs,
    src/containers/sbx/argv.rs,
    src/logging.rs,
    src/session/mod.rs,
    src/agents.rs,
    src/hooks/status_file.rs,
    src/containers/sbx_kit/spec.yaml
  </read_first>
  <behavior>
    - `KitMaterializer::ensure("claude")` returns `Ok(<path>)` where `<path>` ends with `/sbx-kit/aoe-<CARGO_PKG_VERSION>-<16hex>/claude/` and contains `spec.yaml` + `install.sh` files.
    - Second call to `ensure("claude")` with no changes is a no-op fast path: the target dir is the same path, file mtimes are unchanged on disk.
    - `ensure("unknown-agent")` (agent absent from `crate::agents::install_hint(...)`) returns `Err(_)` and does NOT create any files (fail-loud per D-10).
    - For each v1 agent in `{"claude","codex","opencode","gemini","droid","pi","qwen","kiro","hermes","vibe"}`, the materialized `install.sh` body contains the agent's `install_hint` literal (e.g., claude returns `npm install -g @anthropic-ai/claude-code`).
    - For each agent without a workable headless install (`cursor`, `copilot`, `settl`), `ensure(<agent>)` returns `Err(_)` with a clear error message mentioning the agent name and the manual-install requirement.
    - `cache_dir(app_dir, "claude")` is deterministic across two calls with the same inputs (same path) and changes when the embedded byte sequence changes (verified via the byte-injectable test seam below).
    - Concurrent `ensure(agent)` racers (N=4 threads, shared `tempfile::TempDir` as the app dir) all return `Ok` paths pointing at the same final dir; no tmp dirs survive after all racers complete.
    - On unix, the materialized `install.sh` has mode `0o755` after `ensure` returns.
  </behavior>
  <action>
    Edit `src/containers/sbx/mod.rs`: add `pub mod kit;` next to the existing `pub mod argv;` (line 7). No other changes to mod.rs.

    Create `src/containers/sbx/kit.rs` containing:

    1. Module-level docstring (3-5 lines) explaining the materializer's contract: lazy on first sbx session, atomic-rename for concurrency, content-addressed cache key, embedded via stdlib `include_str!`. Reference CONTEXT.md D-08..D-13 by ID in the docstring so future readers find them. No `--` separators.

    2. Module-private constants embedding all seven kit asset files via `include_str!("../sbx_kit/<file>")`:
       - `SPEC_YAML`, `HOOK_SHIM`, `GITIGNORE_AOE_HOOKS`.
       - `SETTINGS_CLAUDE`, `SETTINGS_GEMINI`, `SETTINGS_CURSOR`, `SETTINGS_QWEN`.
       Each `const NAME: &str = include_str!(...)`. Compile-time intrinsic; no runtime cost.

    3. `fn embedded_kit_bytes() -> Vec<u8>` concatenates the seven constants' `.as_bytes()` in declared order. This is the input to `Sha256` and the byte stream the spec_hash is computed over (D-08, D-13).

    4. `pub(crate) fn cache_dir(app_dir: &Path, agent: &str) -> PathBuf` returns `<app_dir>/sbx-kit/aoe-<CARGO_PKG_VERSION>-<hash16>/<agent>/`. Compute hash as `sha2::Sha256::digest(embedded_kit_bytes())`, format as lowercase hex via `format!("{:x}", hash)`, take `&hash_hex[..16]` (RESEARCH A5; 64-bit collision resistance is sufficient). `pub(crate)` so the integration test can call it directly. Internally delegates to `cache_dir_inner(app_dir, agent, &embedded_kit_bytes())`.

       For test seams, expose ONE module-private helper `fn cache_dir_inner(app_dir: &Path, agent: &str, bytes: &[u8]) -> PathBuf` at module-private visibility (no `pub`/`pub(crate)`); the in-module `#[cfg(test)] mod tests` block at the bottom of kit.rs has direct access to module-private items, so a test can invoke `cache_dir_inner` with arbitrary bytes without exposing a public test surface. This satisfies AGENTS.md's "no dead code / no test-only public surface" rule: `cache_dir_inner` is consumed by both `cache_dir` (production) and the byte-flip test (in-module).

    5. `pub fn ensure(agent: &str) -> anyhow::Result<PathBuf>` is the public entry point. Implementation: thin wrapper around the inner helper `ensure_with_app_dir(app_dir: &Path, agent: &str) -> anyhow::Result<PathBuf>`. Public surface: `pub fn ensure(agent: &str) -> anyhow::Result<PathBuf> { let app_dir = crate::session::get_app_dir()?; ensure_with_app_dir(&app_dir, agent) }`.

       `ensure_with_app_dir` is module-private (no `pub`/`pub(crate)`); the in-module tests call it directly via name resolution. No test-only public seam. Steps:
       - Compute `target = cache_dir(app_dir, agent)`.
       - Fast path: if `target.exists()`, return `Ok(target)` immediately (D-11, D-12). No GC fires on cache hit (D-15, RESEARCH Pitfall 6); GC wiring lives in Task 2b.
       - Verify the agent is supported. Pull the install_hint via `crate::agents::install_hint(agent).ok_or_else(|| anyhow::anyhow!("agent '{}' has no install_hint; not supported in sbx kit v1", agent))?`. For the v1 unsupported subset (cursor, copilot, settl), the install body is wrapped to fail loud at install time; per D-10 the planner ships this subset as "ensure returns Err immediately" so the user fails fast at `aoe add` time rather than at the in-VM install hook. Implementation: a `const UNSUPPORTED_AGENTS: &[&str] = &["cursor", "copilot", "settl"];` early-return check before calling `install_hint`.
       - Compose tmp dir path: `<app_dir>/sbx-kit/aoe-<ver>-<hash16>/<agent>.tmp.<pid>.<nanos>/` (NOT `target.with_extension(...)`; directories don't have file extensions; build via `target.parent().unwrap().join(format!("{}.tmp.{}.{}", agent, pid, nanos))`).
       - `create_dir_all(&tmp)?`.
       - Write four files into `tmp`:
         - `tmp.join("spec.yaml")` from `SPEC_YAML.as_bytes()` (the embedded spec, byte-for-byte; the materialized form must equal the source under test, D-17).
         - `tmp.join("install.sh")` from a small shell harness: `#!/bin/sh\nset -eu\n<install_hint body>\n`. Use the existing `install_hint` accessor.
         - `tmp.join("hook-shim.sh")` from `HOOK_SHIM.as_bytes()`.
         - `tmp.join("gitignore-aoe-hooks")` from `GITIGNORE_AOE_HOOKS.as_bytes()`.
       - On unix, set `install.sh` mode to `0o755` via `std::os::unix::fs::PermissionsExt::set_mode`. Use a `#[cfg(unix)]` block mirroring the in-tree precedent at `src/logging.rs:461-465` (PermissionsExt for a file-permission flag; AGENTS.md's "no cfg outside src/process/" rule targets OS-specific behavior dispatch, not permission flags). This is the DETERMINISTIC outcome; no fallback. If the executor cannot apply the chmod (e.g., the underlying fs returns Err), propagate the error rather than silently swallowing it.
       - Rename: `match std::fs::rename(&tmp, &target) { Ok(()) => {}, Err(_) if target.exists() => { let _ = std::fs::remove_dir_all(&tmp); }, Err(e) => { let _ = std::fs::remove_dir_all(&tmp); return Err(anyhow::Error::from(e).context(format!("rename {} -> {}", tmp.display(), target.display()))); } }`. The `_` discard on error kind is intentional (RESEARCH Pitfall 1); use `target.exists()` as the authoritative signal.
       - Return `Ok(target)`. (Task 2b inserts the GC trigger here.)

    6. `#[cfg(test)] mod tests` block, minimum tests:
       - `cache_dir_path_shape`: assert `cache_dir(tempdir.path(), "claude")` ends with `sbx-kit/aoe-<CARGO_PKG_VERSION>-<16hex>/claude/` and that the 16hex portion is lowercase hex.
       - `cache_dir_changes_on_content_byte_flip`: call `cache_dir_inner(tempdir.path(), "claude", b"alpha")` and `cache_dir_inner(tempdir.path(), "claude", b"alphb")`; assert the two paths differ in the hash segment. Verifies the byte-flip invariant without exposing a public test surface.
       - `ensure_creates_files_on_miss`: call `ensure_with_app_dir(tempdir.path(), "claude")`. Assert `target.join("spec.yaml").exists()`, `target.join("install.sh").exists()`, `target.join("hook-shim.sh").exists()`, `target.join("gitignore-aoe-hooks").exists()`. Assert `install.sh` body contains `npm install -g @anthropic-ai/claude-code`.
       - `ensure_idempotent_on_hit`: call `ensure_with_app_dir(tempdir.path(), "claude")` twice; assert returned paths equal; assert the spec.yaml mtime is unchanged between calls (RESEARCH spec.yaml mtime invariance is the fast-path observable).
       - `ensure_fails_loud_for_unsupported`: `ensure_with_app_dir(tempdir.path(), "cursor")` returns Err; the error message contains `"cursor"`; no files are created in the temp dir.
       - `install_sh_contains_install_hint_per_agent`: matrix loop over `["claude","codex","opencode","gemini","droid","pi","qwen","kiro","hermes","vibe"]`; for each, `ensure_with_app_dir(tempdir.path(), agent)` returns Ok and the materialized `install.sh` body contains the agent's `install_hint` substring.
       - `spec_yaml_hook_shim_uses_workspace_path`: parse the materialized `spec.yaml` via `serde_yaml::from_str::<serde_yaml::Value>`, walk `initFiles`, and assert at least one entry's `content` substring contains `${WORKDIR}/.aoe-hooks/$AOE_INSTANCE_ID/status`.
       - `spec_yaml_no_workdir_in_paths`: walk `initFiles`, assert no entry's `path` contains `${WORKDIR}` (RESEARCH Pitfall 5).
       - `spec_yaml_gitignore_drop_in_commands_install`: assert `spec["commands"]["install"]` (sequence) contains an entry whose extracted command substring contains `.aoe-hooks/` and `grep -qsE` (the idempotency guard).
       - `spec_yaml_declares_credentials_sources_ssh_agent`: parse the materialized `spec.yaml`; assert `spec["credentials"]["sources"]` is a mapping and that traversing it yields at least one leaf string equal to `"ssh-agent"` (D-05, [ASSUMED] schema; the test pins the current best-effort shape so a regression that drops the block is caught, even though the exact field path may shift after the live-sbx smoke test in VALIDATION.md).
       - `concurrent_ensure_all_succeed`: `std::thread::scope` with 4 threads, each calling `ensure_with_app_dir(shared_tempdir.path(), "claude")`; collect results; assert all `Ok(path)` and all paths equal; assert no `.tmp.` dirs remain under `<tempdir>/sbx-kit/aoe-*/`.
       - `embedded_bytes_match_source`: `assert_eq!(SPEC_YAML, std::fs::read_to_string("src/containers/sbx_kit/spec.yaml").unwrap())`, proves include_str! grabs the right file (this is the KIT-01 "embedded bytes match source" check from VALIDATION).
       - `install_sh_is_executable_on_unix` (gated on `#[cfg(unix)]`): call `ensure_with_app_dir(tempdir.path(), "claude")`, read `install.sh` metadata via `std::fs::metadata(target.join("install.sh"))?.permissions()`, assert `permissions.mode() & 0o777 == 0o755`. Pulls in `use std::os::unix::fs::PermissionsExt;` inside the test block.
       - `ensure_kit_path_threads_through_build_create_args`: this lives in `src/containers/sbx/argv.rs::tests` (already exists from Phase 2). Add a new test there that calls `kit::ensure_with_app_dir(tempdir.path(), "claude")?`, passes the path as `Some(&path)` to `build_create_args`, and asserts the resulting argv contains `["--kit", <path-as-string>, "--template", "alpine:latest"]` in that order. This satisfies KIT-02 SC-5. Note: `ensure_with_app_dir` is module-private to kit.rs, so the argv test must be inside `src/containers/sbx/kit.rs::tests` instead (call build_create_args via `crate::containers::sbx::argv::build_create_args`) to keep the test-only surface contained. Rename the test accordingly: `kit_path_threads_through_build_create_args` inside kit.rs::tests.

    7. Test helper layout: place a `fn temp_app_dir() -> tempfile::TempDir` helper inside `mod tests` so each test owns its own scope and the dir is auto-cleaned on drop.

    8. Per AGENTS.md: no dead code; every module-private helper (`cache_dir_inner`, `ensure_with_app_dir`) is called by production code AND by the in-module tests, so neither is test-only public surface. No `#[allow(dead_code)]`. No `--` separators in doc comments or `eprintln!` strings; the `||` and `&&` shell guards inside string literals are CLI syntax, not prose separators. The acceptance criteria below add an explicit grep gate for em-dash and `--` separators in human-authored prose only.
  </action>
  <verify>
    <automated>cargo test --lib containers::sbx::kit 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build --lib` exits 0; `cargo clippy --lib -- -D warnings` exits 0.
    - `src/containers/sbx/mod.rs` contains the line `pub mod kit;` (in addition to existing `pub mod argv;`).
    - `grep -c 'include_str!' src/containers/sbx/kit.rs` returns at least 7.
    - `grep -c 'pub fn ensure' src/containers/sbx/kit.rs` returns 1 (the public entry).
    - `grep -cE '#\[allow\(dead_code\)\]' src/containers/sbx/kit.rs` returns 0.
    - `grep -nE '— | --[^-]' src/containers/sbx/kit.rs src/containers/sbx_kit/spec.yaml | grep -vE '"(--[a-z-]+|sh -c)"' | wc -l` returns 0 (no em-dash or `--` separators in human-authored prose; CLI flag literals like `"--kit"` and the `sh -c` shell invocation are exempt).
    - `cargo test --lib containers::sbx::kit::tests` exits 0 and reports at least 12 passing tests.
    - `cargo test --lib containers::sbx::kit::tests::ensure_idempotent_on_hit` exits 0 (covers VALIDATION KIT-01 cache-hit no-op).
    - `cargo test --lib containers::sbx::kit::tests::concurrent_ensure_all_succeed` exits 0 (covers D-12 / KIT-05 race).
    - `cargo test --lib containers::sbx::kit::tests::ensure_fails_loud_for_unsupported` exits 0 (covers D-10).
    - `cargo test --lib containers::sbx::kit::tests::install_sh_is_executable_on_unix` exits 0 on unix CI (Warning 9; explicit assertion that the chmod 0o755 lands).
    - `cargo test --lib containers::sbx::kit::tests::spec_yaml_declares_credentials_sources_ssh_agent` exits 0 (D-05).
    - `cargo test --lib containers::sbx::kit::tests::kit_path_threads_through_build_create_args` exits 0 (covers KIT-02 SC-5).
  </acceptance_criteria>
  <done>
    `src/containers/sbx/kit.rs` exists with the materializer surface (`ensure`, module-private `ensure_with_app_dir`, `cache_dir`, module-private `cache_dir_inner`, `embedded_kit_bytes`) and in-module tests; no test-only public surface. `src/containers/sbx/mod.rs` registers the new sub-module. `install.sh` lands with 0o755 on unix and is asserted by `install_sh_is_executable_on_unix`. Credentials block landed and pinned by `spec_yaml_declares_credentials_sources_ssh_agent`. All listed tests pass under `cargo test --lib containers::sbx::kit`. `cargo clippy --lib -- -D warnings` is clean. GC code lives in Task 2b.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2b: Add gc_stale_kit_dirs background sweep + wire fire-and-forget GC into ensure</name>
  <files>
    src/containers/sbx/kit.rs
  </files>
  <read_first>
    src/containers/sbx/kit.rs,
    src/logging.rs,
    src/session/mod.rs
  </read_first>
  <depends_on>
    - "Task 2a (within Plan 04-01): ensure_with_app_dir + cache_dir must exist before GC can be wired into the cache-miss branch"
  </depends_on>
  <behavior>
    - `gc_stale_kit_dirs(root, Duration::from_secs(30*24*3600))` removes `aoe-*` top-level entries with mtime older than 30 days, leaves fresh ones alone, ignores `.tmp.*` suffixed dirs, and treats non-`aoe-` siblings as inert (no removal).
    - `gc_stale_kit_dirs` returns `Result<(), std::io::Error>`; per-entry failures log via `tracing::info!` and continue; the outer Result is for `read_dir` failures of `root` itself.
    - After `ensure_with_app_dir` writes a fresh dir on cache miss, a `tokio::task::spawn_blocking` fires the GC sweep against `<app_dir>/sbx-kit/`. The handle is dropped (fire-and-forget per D-15). GC errors are NEVER propagated to `ensure`'s caller; failures log at `tracing::info!` and continue.
    - GC fires ONLY on the cache-miss branch (never on cache hit; D-15 / RESEARCH Pitfall 6).
  </behavior>
  <action>
    Edit `src/containers/sbx/kit.rs` (created in Task 2a) and add:

    1. `fn gc_stale_kit_dirs(root: &Path, max_age: Duration) -> std::io::Result<()>` at module-private visibility. One-level enumeration of `root` via `std::fs::read_dir`. For each entry:
       - Skip if the filename does not start with `"aoe-"` (defends `root` against arbitrary file deletion; T-04-01-02 mitigation).
       - Skip if the filename contains `".tmp."` (mid-rename racer; never delete; T-04-01-02 mitigation).
       - Read `entry.metadata()?.modified()?`; if `now.duration_since(mtime).unwrap_or(Duration::ZERO) > max_age`, call `std::fs::remove_dir_all(&path)`. On Ok, log via `tracing::info!(target: "containers.sbx.kit", path = %path.display(), "removed stale kit cache dir")`. On Err, log via `tracing::info!(target: "containers.sbx.kit", error = %e, path = %path.display(), "stale-kit GC: remove failed (non-fatal)")` and continue to the next entry; never propagate per-entry failure.
       - Returns `Ok(())` once the read_dir iteration completes. If `read_dir(root)` itself fails (e.g., root does not exist on first-ever materialization), return the Err so `ensure`'s caller can log it once.

    2. Wire GC into `ensure_with_app_dir`'s cache-miss branch (immediately before the final `Ok(target)` return, AFTER the rename succeeded):
       ```
       let gc_root = app_dir.join("sbx-kit");
       tokio::task::spawn_blocking(move || {
           if let Err(e) = gc_stale_kit_dirs(&gc_root, Duration::from_secs(30 * 24 * 3600)) {
               tracing::info!(target: "containers.sbx.kit", error = %e, "stale-kit GC skipped (non-fatal)");
           }
       });
       ```
       The JoinHandle is dropped (fire-and-forget per D-15). GC fires ONLY in this branch, never in the fast-path return at the top of `ensure_with_app_dir`.

    3. Add tests to the existing `#[cfg(test)] mod tests` block in kit.rs:
       - `gc_removes_only_stale_aoe_dirs`: build a fixture `<root>/aoe-old/` and `<root>/aoe-fresh/`. Set `aoe-old`'s mtime to 31 days ago via `std::fs::File::open(&dir)?.set_modified(SystemTime::now() - Duration::from_secs(31*24*3600))?` (stable since Rust 1.75; if the test target's MSRV blocks this, fall back to creating `aoe-old` then invoking GC with `Duration::from_secs(0)` and asserting removal). Call `gc_stale_kit_dirs(&root, Duration::from_secs(30*24*3600))`. Assert post-GC `aoe-old` is gone, `aoe-fresh` remains.
       - `gc_refuses_to_delete_non_aoe_siblings`: build `<root>/not-aoe-dir/` AND `<root>/aoe-old-fresh-mtime-but-name-prefix-only/` (testing two separate guards: the prefix check + arbitrary siblings). Set `not-aoe-dir`'s mtime to 99 days ago. Call `gc_stale_kit_dirs(&root, Duration::from_secs(30*24*3600))`. Assert `not-aoe-dir` still exists (security/path-traversal mitigation per T-04-01-02).
       - `gc_skips_tmp_suffixed_dirs`: build `<root>/aoe-X.tmp.123.456/` with mtime set 99 days ago. Call GC. Assert it survives (`.tmp.` guard fires before the age check; T-04-01-02 mitigation against deleting a mid-rename racer).
       - `gc_logs_and_continues_on_per_entry_failure`: this one cannot easily be exercised without a permission-fault injection. Skip the test; rely on the fact that the `tracing::info!` + `continue` pattern is inspectable in code review. Document this gap inline above the GC fn.
       - `ensure_with_app_dir_fires_gc_only_on_miss`: this also cannot be exercised without a stub for `tokio::task::spawn_blocking`. Skip; document inline that GC firing is verified by inspection plus the integration-test pattern in Phase 5.

    4. Per AGENTS.md: no dead code; `gc_stale_kit_dirs` is consumed by `ensure_with_app_dir` (production) and by three in-module tests. No new external deps. No `--` prose separators in doc comments.
  </action>
  <verify>
    <automated>cargo test --lib containers::sbx::kit 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'fn gc_stale_kit_dirs' src/containers/sbx/kit.rs` returns 1.
    - `grep -c 'spawn_blocking' src/containers/sbx/kit.rs` returns 1 (single GC trigger point; lives in the cache-miss branch).
    - `grep -c 'starts_with("aoe-")' src/containers/sbx/kit.rs` returns at least 1 (path-traversal guard).
    - `grep -c '".tmp."' src/containers/sbx/kit.rs` returns at least 1 (mid-rename guard).
    - `cargo test --lib containers::sbx::kit::tests::gc_removes_only_stale_aoe_dirs` exits 0.
    - `cargo test --lib containers::sbx::kit::tests::gc_refuses_to_delete_non_aoe_siblings` exits 0.
    - `cargo test --lib containers::sbx::kit::tests::gc_skips_tmp_suffixed_dirs` exits 0.
    - `cargo test --lib containers::sbx::kit` exits 0 with all Task 2a tests + 3 new GC tests passing.
    - `cargo clippy --lib -- -D warnings` is clean.
  </acceptance_criteria>
  <done>
    `gc_stale_kit_dirs` exists in src/containers/sbx/kit.rs with the prefix + `.tmp.` guards and per-entry log-and-continue error handling. `ensure_with_app_dir` fires the sweep via `tokio::task::spawn_blocking` only on cache miss; the JoinHandle is dropped. The three GC tests pass: stale removed, fresh kept, non-`aoe-` siblings + `.tmp.` dirs untouched. Clippy clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| compile-time, aoe binary | Static kit assets at `src/containers/sbx_kit/*` are embedded via `include_str!` and become read-only `&'static str` in the running binary. No runtime mutation path exists; threats here are pre-merge code-review concerns. |
| aoe binary, host filesystem (`<app_dir>/sbx-kit/`) | Materializer writes per-agent kit dirs. Only `aoe` (running as the host user) writes here; only `aoe` reads. Multiple `aoe` processes may race (D-12). |
| `<app_dir>/sbx-kit/`, sbx microVM | sbx daemon reads the materialized dir at `sbx create --kit <path>` time. aoe trusts sbx's parser. Phase 4 does not invoke sbx. |
| sbx microVM workspace, host workspace | Inside the VM, hook command writes `${WORKDIR}/.aoe-hooks/<id>/status`. Workspace mount is the user's repo; if the workspace is owned by the user, the agent inside the VM (uid 1000) has write access through the mount. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01-01 | Tampering | `KitMaterializer::ensure(agent)` | medium | mitigate | `agent` is a `&str` joined into a `PathBuf` via `Path::join`. An attacker-controlled agent name (e.g., `"../../etc/passwd"`) could escape the cache root. **Mitigation**: validate agent name against the registry. Implementation: `crate::agents::install_hint(agent)` returns `Some(_)` only for registered agents (registry is a static `&'static [AgentDef]` at compile time; names are bounded `[a-z0-9-]+`). Adding an explicit allowlist regex would be redundant since `install_hint`'s `Some` arm is the gate. Acceptance: the unsupported-agent test asserts `ensure_with_app_dir(temp, "../etc")` returns Err and creates no files. |
| T-04-01-02 | Tampering | `gc_stale_kit_dirs` | high | mitigate | Walks `<app_dir>/sbx-kit/` and `remove_dir_all` entries. A bug that fails the `starts_with("aoe-")` filter or follows a symlink could delete arbitrary files under the app dir. **Mitigation**: explicit `if !filename.starts_with("aoe-") { continue; }` guard; explicit `if filename.contains(".tmp.") { continue; }` guard; never follow into siblings; `std::fs::remove_dir_all` does not traverse symlinks beyond the entry point per stdlib docs. Acceptance: `gc_refuses_to_delete_non_aoe_siblings` test asserts non-`aoe-` dirs survive; `gc_skips_tmp_suffixed_dirs` test asserts mid-rename dirs survive. |
| T-04-01-03 | Tampering | `install.sh` body via `install_hint` | low | accept | The `install_hint` value comes from `src/agents.rs::AgentDef::install_hint: &'static str`, a compile-time constant authored by the project maintainers. Treated as trusted in-binary data. **Disposition**: accept. Documenting the trust boundary explicitly here so a future change that derives `install_hint` from user input (e.g., a config file) gets flagged for re-review. |
| T-04-01-04 | Tampering | Hook shim command inside materialized `spec.yaml` | low | accept | The shim uses `$AOE_INSTANCE_ID` from `sbx exec -e AOE_INSTANCE_ID=<id>`. `<id>` is generated by `Instance::new` (UUID-shaped). If a future change makes the id user-derived, the shell command becomes a shell-injection vector. **Disposition**: accept now; the in-VM `sh -c '...'` quoting around `$AOE_INSTANCE_ID` (`mkdir -p "${WORKDIR}/.aoe-hooks/$AOE_INSTANCE_ID"`) provides single-layer defense. Future-flag: if `instance.id` ever becomes attacker-influenceable, validate against `[A-Za-z0-9-]+` at the call site. |
| T-04-01-05 | Information Disclosure | `<workspace>/.aoe-hooks/` | medium | mitigate | Hook status files land in the user's workspace and could be committed. **Mitigation**: spec.yaml's `commands.install` writes `.aoe-hooks/` into `${WORKDIR}/.gitignore` idempotently (guarded by `grep -qsE '^\.aoe-hooks/?$' ...`). Acceptance: `spec_yaml_gitignore_drop_in_commands_install` test asserts the guard pattern + the `.aoe-hooks/` line are present in the install hook string. 04-02 adds a follow-up assertion that runs the install hook against a tmp workspace fixture to verify idempotency in shell rather than via static substring. |
| T-04-01-06 | Denial of Service | `gc_stale_kit_dirs` on cache miss | low | mitigate | A symlink loop or extremely large stale tree could stall `remove_dir_all`. **Mitigation**: GC runs in `tokio::task::spawn_blocking` (not on the create_container critical path); the JoinHandle is dropped (fire-and-forget per D-15). A blocked sweep does not block `ensure` from returning. Acceptance: no test, but the spawn_blocking pattern is verified by inspection of `ensure_with_app_dir`. |
| T-04-01-07 | Tampering | Supply chain (no new packages) | low | accept | Phase 4 adds zero new dependencies. `sha2`, `serde_yaml`, `tokio`, `tempfile`, `tracing`, `anyhow` are inherited from Phase 1-3 stack and previously audited. No `[ASSUMED]` / `[SUS]` packages. No legitimacy checkpoint required. |
| T-04-01-08 | Tampering | `credentials.sources` ssh-agent block ([ASSUMED] schema) | medium | mitigate | The schema for `credentials.sources.<svc>.agent: ssh-agent` is undocumented (RESEARCH A1). A wrong schema means sbx may reject the kit at create time OR (worse) silently ignore the block and ship without SSH forwarding. **Mitigation**: VALIDATION.md row `credentials.sources ssh-agent schema` requires a live `sbx create shell --kit <materialized> --template <image>` + `ssh-add -L` smoke test before phase exit. Inline YAML comment in spec.yaml flags the assumption for the next reader. Acceptance: `spec_yaml_declares_credentials_sources_ssh_agent` test pins the current shape so a regression is caught; live-sbx confirmation in VALIDATION.md closes the assumption. |
</threat_model>

<verification>
- `cargo build --lib` and `cargo build --features serve` both succeed.
- `cargo clippy --lib -- -D warnings` passes (no new warnings introduced).
- `cargo test --lib containers::sbx::kit` passes with all listed tests (Task 2a + Task 2b).
- `cargo fmt --all -- --check` passes.
- `grep -rE '\#\[allow\(dead_code\)\]' src/containers/sbx/kit.rs src/containers/sbx_kit/` returns nothing.
</verification>

<success_criteria>
- KIT-01 satisfied: embedded kit assets exist at `src/containers/sbx_kit/`; `include_str!` constants in `kit.rs` cover all seven assets; `cache_dir` returns `<app_dir>/sbx-kit/aoe-<CARGO_PKG_VERSION>-<hash16>/<agent>/`; `ensure` materializes lazily on first call and is a no-op on cache hit; `tempfile`-backed tests cover both paths.
- KIT-02 satisfied: `src/containers/sbx/kit.rs::tests::kit_path_threads_through_build_create_args` proves the materialized path feeds `build_create_args(name, image, Some(&materialized), &config)` producing the `sbx create shell --kit <path> --template <image>` argv shape.
- KIT-03 satisfied: per-agent `install.sh` body contains `crate::agents::install_hint(agent)` literal for the v1 subset; unsupported agents return Err from `ensure`. D-05 honored: `credentials.sources` block declares ssh-agent forwarding ([ASSUMED] schema; live-sbx confirmation tracked in VALIDATION.md).
- KIT-04 (partial; in-VM half): `spec.yaml`'s `initFiles` writes the hook shim into agent settings.json at `${WORKDIR}/.aoe-hooks/$AOE_INSTANCE_ID/status`; `${WORKDIR}` appears in `content:` only; `commands.install` drops `.aoe-hooks/` into the workspace `.gitignore` idempotently. (Host-side dispatch + the workspace-gitignore correctness assertion land in 04-02.)
- KIT-05 satisfied: cache key includes both `CARGO_PKG_VERSION` and the SHA256 of embedded bytes; mutating one byte changes the path; 30-day GC sweeps stale `aoe-*` top-level dirs in `tokio::task::spawn_blocking` triggered ONLY on cache miss; tests cover both the GC age filter and the `.tmp.` skip + non-`aoe-` skip safety rails.
</success_criteria>

<output>
Create `.planning/phases/04-embedded-sbx-kit-and-materialization/04-01-SUMMARY.md` when done, following the standard summary template.
</output>
