---
phase: 04-embedded-sbx-kit-and-materialization
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/hooks/status_file.rs
  - src/hooks/mod.rs
  - xtask/src/main.rs
  - .github/workflows/ci.yml
  - tests/xtask_check_kit.rs
  - tests/fixtures/sbx_kits/ok/spec.yaml
  - tests/fixtures/sbx_kits/missing-schema-version/spec.yaml
  - tests/fixtures/sbx_kits/naked-install-verb/spec.yaml
  - tests/fixtures/sbx_kits/guarded-install-verb/spec.yaml
autonomous: true
requirements:
  - KIT-04
  - KIT-06

must_haves:
  truths:
    - "src/hooks/status_file.rs exposes a NEW free function `sbx_hook_status_dir(project_path: &Path, instance_id: &str) -> PathBuf` returning `<project_path>/.aoe-hooks/<instance_id>` (D-02)"
    - "src/hooks/status_file.rs exposes a NEW pure function `resolve_status_dir(supports_arbitrary_volume_paths: bool, project_path: &Path, instance_id: &str) -> PathBuf` that dispatches between `hook_status_dir` and `sbx_hook_status_dir` based on the capability flag; the dispatch shim collapses to a one-liner at the call site (D-03)"
    - "The existing `hook_status_dir(instance_id)`, `read_hook_status(instance_id)`, `cleanup_hook_status_dir(instance_id)` stay UNCHANGED in shape and behavior for Docker/Podman/AppleContainer (D-02; no rename)"
    - "A single new dispatch shim in `src/hooks/status_file.rs` selects between `hook_status_dir` and `sbx_hook_status_dir` based on `runtime.capabilities().supports_arbitrary_volume_paths` (D-03)"
    - "`cargo xtask check-kit` exits 0 when the embedded spec.yaml is well-formed and idempotency-guarded (D-16, D-17)"
    - "`cargo xtask check-kit` exits non-zero with a clear error message when: schemaVersion missing or not \"1\", required field absent, OR commands.startup contains a naked install verb without an idempotency guard (D-16)"
    - "`.github/workflows/ci.yml` runs `cargo xtask check-kit` on every push, mirroring the existing `skill:` job (D-16)"
    - "The new dispatch shim is consumed at the four existing `read_hook_status` call sites (tui/app.rs:1055, server/api/sessions.rs:1095, cli/session.rs:535, session/instance.rs:1603, session/instance.rs:1761) AND the one `cleanup_hook_status_dir` call site (session/deletion.rs:300; instance.rs:1642 is internal cleanup on stop and is also routed through the new shim)"
  artifacts:
    - path: "src/hooks/status_file.rs"
      provides: "Existing trio (hook_status_dir, read_hook_status, cleanup_hook_status_dir) + new sibling sbx_hook_status_dir + new pure resolve_status_dir dispatcher + new dispatch shims read_hook_status_for_instance / cleanup_hook_status_dir_for_instance"
      contains: "pub fn sbx_hook_status_dir"
    - path: "src/hooks/mod.rs"
      provides: "Re-export of the new `sbx_hook_status_dir` + dispatch shims alongside the existing trio"
      contains: "sbx_hook_status_dir"
    - path: "xtask/src/main.rs"
      provides: "New `CheckKit` Commands variant + `fn check_kit()` parsing the embedded spec.yaml directly via include_str!"
      contains: "CheckKit"
    - path: ".github/workflows/ci.yml"
      provides: "New `kit:` job (peer to the `skill:` job) running `cargo xtask check-kit`"
      contains: "cargo xtask check-kit"
    - path: "tests/xtask_check_kit.rs"
      provides: "Integration test invoking the xtask binary against fixture spec.yaml files via std::process::Command"
    - path: "tests/fixtures/sbx_kits/ok/spec.yaml"
      provides: "Fixture: valid spec.yaml (exits 0)"
    - path: "tests/fixtures/sbx_kits/missing-schema-version/spec.yaml"
      provides: "Fixture: missing schemaVersion (exits 1)"
    - path: "tests/fixtures/sbx_kits/naked-install-verb/spec.yaml"
      provides: "Fixture: naked `apt-get install` in commands.startup (exits 1)"
    - path: "tests/fixtures/sbx_kits/guarded-install-verb/spec.yaml"
      provides: "Fixture: `command -v foo || apt-get install foo` in commands.startup (exits 0)"
  key_links:
    - from: "src/hooks/status_file.rs::sbx_hook_status_dir"
      to: "instance.project_path"
      via: "Direct &Path argument; caller passes Path::new(&instance.project_path)"
      pattern: "sbx_hook_status_dir\\(Path::new"
    - from: "src/hooks/status_file.rs (dispatch shim)"
      to: "runtime.capabilities().supports_arbitrary_volume_paths"
      via: "containers::get_container_runtime().capabilities()"
      pattern: "supports_arbitrary_volume_paths"
    - from: "src/hooks/status_file.rs::resolve_status_dir"
      to: "hook_status_dir / sbx_hook_status_dir"
      via: "Pure function branching on the bool capability flag; called from both _for_instance shims"
      pattern: "resolve_status_dir\\("
    - from: "xtask/src/main.rs::check_kit"
      to: "src/containers/sbx_kit/spec.yaml"
      via: "include_str! at compile time inside the xtask binary"
      pattern: "include_str!\\(\".*sbx_kit/spec\\.yaml\"\\)"
    - from: ".github/workflows/ci.yml kit job"
      to: "cargo xtask check-kit"
      via: "Single `run:` step"
      pattern: "cargo xtask check-kit"
---

<objective>
Close the host side of KIT-04 and all of KIT-06. Add the sibling `sbx_hook_status_dir` free function, a pure `resolve_status_dir` dispatcher with unit-test coverage, route the existing hook-status call sites through a single capability-driven dispatch shim, ship `cargo xtask check-kit` parsing the embedded spec.yaml from 04-01, and wire it into CI as a peer job to `skill:`.

Purpose: KIT-04 needs both halves (in-VM shim writes workspace-relative, owned by 04-01; host poller reads workspace-relative, owned here). KIT-06 needs both the xtask subcommand and the CI step. 04-02 cannot start until 04-01 produces the embedded spec.yaml that `cargo xtask check-kit` parses.

Output: One sibling free fn + one pure dispatcher + two dispatch shims in `src/hooks/status_file.rs`; one re-export update in `src/hooks/mod.rs`; a `CheckKit` variant + `check_kit()` function in `xtask/src/main.rs`; one new `kit:` job in `.github/workflows/ci.yml`; one integration test file plus four fixture spec.yaml files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-embedded-sbx-kit-and-materialization/04-CONTEXT.md
@.planning/phases/04-embedded-sbx-kit-and-materialization/04-RESEARCH.md
@.planning/phases/04-embedded-sbx-kit-and-materialization/04-PATTERNS.md
@.planning/phases/04-embedded-sbx-kit-and-materialization/04-VALIDATION.md
@.planning/phases/04-embedded-sbx-kit-and-materialization/04-01-PLAN.md

@src/hooks/status_file.rs
@src/hooks/mod.rs
@src/containers/runtime.rs
@src/containers/container_interface.rs
@src/containers/runtime_base.rs
@src/session/instance.rs
@src/tui/app.rs
@src/server/api/sessions.rs
@src/cli/session.rs
@src/session/deletion.rs
@xtask/src/main.rs
@.github/workflows/ci.yml
@src/containers/sbx_kit/spec.yaml

<interfaces>
<!-- Extracted from codebase. Executor consumes these directly. -->

Existing trio in src/hooks/status_file.rs (DO NOT rename, DO NOT change shape per CONTEXT D-02):
```rust
pub fn hook_status_dir(instance_id: &str) -> PathBuf;
pub fn read_hook_status(instance_id: &str) -> Option<Status>;
pub fn cleanup_hook_status_dir(instance_id: &str);
```
Re-exported from src/hooks/mod.rs line 16.

Existing call sites of `read_hook_status` (5 sites; 04-02 routes through the new shim):
- src/tui/app.rs:1055, has `instance: &Instance` in scope (project_path field is `String`).
- src/server/api/sessions.rs:1095, has `decision_instance` in scope.
- src/cli/session.rs:535, has `inst` in scope.
- src/session/instance.rs:1603, `&self.id` is the instance id; `&self.project_path` is the project path.
- src/session/instance.rs:1761, same self.

Existing call sites of `cleanup_hook_status_dir`:
- src/session/instance.rs:1642, `Self::stop` body.
- src/session/deletion.rs:300, `request.instance.id` and `request.instance.project_path` both in scope.

`Instance.project_path: String` (src/session/instance.rs:184). `Path::new(&instance.project_path)` to lift into a `&Path`.

Runtime capability accessor (src/containers/runtime.rs:110):
```rust
fn capabilities(&self) -> RuntimeCapabilities;
```
Returned struct (src/containers/container_interface.rs:59-86) has `supports_arbitrary_volume_paths: bool` among others. `Copy + PartialEq + Eq`.

`crate::containers::get_container_runtime()` is the single factory (src/containers/mod.rs:27); call sites already use this pattern.

Existing xtask shape (xtask/src/main.rs):
```rust
#[derive(Subcommand)]
enum Commands {
    GenDocs,
    CheckSkill,
}

fn main() {
    let args = Xtask::parse();
    match args.command {
        Commands::GenDocs => generate_cli_docs(),
        Commands::CheckSkill => check_skill(),
    }
}
```
`check_skill()` body pattern (xtask/src/main.rs:59): accumulate `has_error: bool` from `eprintln!` violations, `std::process::exit(1)` once at the end, `println!("... passed.")` on success.

`serde_yaml = "0.9"` is in `Cargo.toml:34` AND already in `xtask/Cargo.toml`'s transitive deps (no addition required; if missing as a direct xtask dep, add to xtask/Cargo.toml). The xtask binary is built with its own Cargo.toml at `xtask/Cargo.toml`; verify before writing the parser code.

Existing CI `skill:` job (`.github/workflows/ci.yml:82-93`):
```yaml
  skill:
    name: Skill Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4
        with:
          cache-bin: "false"
          save-if: ${{ github.ref == 'refs/heads/main' }}
      - name: Check skill file references valid CLI commands
        run: cargo xtask check-skill
```
The new `kit:` job mirrors this exactly with the same pinned action SHAs.

Wave-1 dependency: 04-01 produces `src/containers/sbx_kit/spec.yaml`. The xtask `check_kit()` body MUST `include_str!("../../src/containers/sbx_kit/spec.yaml")` (relative to xtask/src/main.rs); if 04-01 has not landed, the xtask crate will not compile.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add sbx_hook_status_dir + resolve_status_dir pure dispatcher + dispatch shims + wire through all call sites</name>
  <files>
    src/hooks/status_file.rs,
    src/hooks/mod.rs,
    src/tui/app.rs,
    src/server/api/sessions.rs,
    src/cli/session.rs,
    src/session/instance.rs,
    src/session/deletion.rs
  </files>
  <read_first>
    src/hooks/status_file.rs,
    src/hooks/mod.rs,
    src/containers/runtime.rs,
    src/containers/runtime_base.rs,
    src/containers/container_interface.rs,
    src/session/instance.rs,
    src/tui/app.rs,
    src/server/api/sessions.rs,
    src/cli/session.rs,
    src/session/deletion.rs
  </read_first>
  <behavior>
    - `sbx_hook_status_dir(Path::new("/tmp/projects/foo"), "instance-abc")` returns the exact `PathBuf` `/tmp/projects/foo/.aoe-hooks/instance-abc`.
    - `resolve_status_dir(true, Path::new("/proj"), "id-x")` returns the legacy `/tmp/aoe-hooks/id-x` path (the same `hook_status_dir(id-x)` produces).
    - `resolve_status_dir(false, Path::new("/proj"), "id-x")` returns `/proj/.aoe-hooks/id-x` (the same `sbx_hook_status_dir(Path::new("/proj"), "id-x")` produces).
    - `read_hook_status_for_instance(instance)` returns `Some(Status::Running)` when the resolved runtime is Docker (capability matrix `supports_arbitrary_volume_paths=true`) AND `/tmp/aoe-hooks/<instance.id>/status` contains `running`; returns `None` when the file is absent.
    - `read_hook_status_for_instance(instance)` returns `Some(Status::Running)` when the resolved runtime reports `!supports_arbitrary_volume_paths` AND `<instance.project_path>/.aoe-hooks/<instance.id>/status` contains `running`.
    - Existing `read_hook_status(instance_id)`, `hook_status_dir(instance_id)`, `cleanup_hook_status_dir(instance_id)` still compile and still return their pre-04-02 results unchanged (no rename, no signature change).
    - All five `read_hook_status` call sites use the new shim (which takes `&Instance`), passing the instance reference rather than just `&id`.
    - Both `cleanup_hook_status_dir` call sites use the new cleanup dispatch shim (`cleanup_hook_status_dir_for_instance(instance)`).
    - `cargo build --features serve` and `cargo build` (TUI-only) both succeed without warnings.
  </behavior>
  <action>
    Sub-step A. Add the sibling free function in `src/hooks/status_file.rs`:
    Below the existing `hook_status_dir` (line 13), add:

    Function name: `sbx_hook_status_dir`.
    Visibility: `pub`.
    Signature: `pub fn sbx_hook_status_dir(project_path: &Path, instance_id: &str) -> PathBuf`.
    Body: `project_path.join(".aoe-hooks").join(instance_id)`.
    Doc-comment: one-paragraph explanation citing CONTEXT.md D-02 (workspace-relative because `!supports_arbitrary_volume_paths` runtimes cannot mount host `/tmp/aoe-hooks` into the microVM; under sbx the workspace mount is provided natively at the same absolute path inside the VM, so the kit's in-VM shim writes through that mount and the host poller reads on the host side of the same path). No em-dashes. No `--` separators in prose.

    Update the `use` block at the top of `src/hooks/status_file.rs` (line 6) to import `std::path::Path` alongside the existing `PathBuf` (currently imports only `PathBuf`).

    Sub-step B. Add the pure dispatcher in `src/hooks/status_file.rs`:
    Immediately after `sbx_hook_status_dir`, add a free function:

    Function name: `resolve_status_dir`.
    Visibility: `pub`.
    Signature: `pub fn resolve_status_dir(supports_arbitrary_volume_paths: bool, project_path: &Path, instance_id: &str) -> PathBuf`.
    Body:
    ```rust
    if supports_arbitrary_volume_paths {
        hook_status_dir(instance_id)
    } else {
        sbx_hook_status_dir(project_path, instance_id)
    }
    ```
    Doc-comment: one paragraph citing D-03 (single dispatch site for status-dir resolution; pure so it is trivially unit-testable). Note: under sbx the workspace mount is provided natively by the sandbox at the same absolute host path inside the VM, so the in-VM hook shim writes through that mount and the host poller reads through the same path with no host-side hook-volume mount needed.

    The two `_for_instance` shims below (sub-step C) call `resolve_status_dir(caps.supports_arbitrary_volume_paths, Path::new(&instance.project_path), &instance.id)` rather than open-coding the if/else; this collapses the dispatch site to a one-liner per shim and lets unit tests prove the branching without stubbing `get_container_runtime`.

    Sub-step C. Add the dispatch shims in `src/hooks/status_file.rs`:
    Below `resolve_status_dir`, add two new free functions:

    `pub fn read_hook_status_for_instance(instance: &crate::session::Instance) -> Option<Status>`:
    Body resolves the runtime once via `let caps = crate::containers::get_container_runtime().capabilities();`, then:
    ```rust
    let dir = resolve_status_dir(
        caps.supports_arbitrary_volume_paths,
        Path::new(&instance.project_path),
        &instance.id,
    );
    let path = dir.join("status");
    let content = std::fs::read_to_string(&path).ok()?;
    parse_status_content(content.trim())
    ```
    The inline `match s.trim() { "running" => ..., ... }` parsing block from existing `read_hook_status` is extracted into a small module-private helper `fn parse_status_content(s: &str) -> Option<Status>` and called from BOTH `read_hook_status` and `read_hook_status_for_instance`. This removes duplication without renaming the existing public API.

    `pub fn cleanup_hook_status_dir_for_instance(instance: &crate::session::Instance)`:
    Body:
    ```rust
    let caps = crate::containers::get_container_runtime().capabilities();
    let dir = resolve_status_dir(
        caps.supports_arbitrary_volume_paths,
        Path::new(&instance.project_path),
        &instance.id,
    );
    if dir.exists() {
        if let Err(e) = std::fs::remove_dir_all(&dir) {
            tracing::warn!("Failed to cleanup hook status dir {}: {}", dir.display(), e);
        }
    }
    ```

    Sub-step D. Re-export the new names from `src/hooks/mod.rs`:
    Update the `pub use status_file::{...}` line (currently `pub use status_file::{cleanup_hook_status_dir, hook_status_dir, read_hook_status};`) to also export `sbx_hook_status_dir`, `resolve_status_dir`, `read_hook_status_for_instance`, and `cleanup_hook_status_dir_for_instance`. Keep the existing three exports; the call site in `src/session/container_config.rs:1108` uses the legacy `hook_status_dir` directly and that path stays unchanged for Docker/Podman/AppleContainer (the sbx microVM never reaches this code branch because Phase 3's `conform_for_capabilities` post-pass drops mounts whose `container_path != host_path` for `!supports_arbitrary_volume_paths` runtimes; the hook mount survives only when paths match). Under sbx the in-VM shim writes the workspace-relative path through the workspace mount that sbx provides natively, so no host-side hook-volume mount is needed at all for sbx, and the host poller reads through the workspace path via `resolve_status_dir`.

    Sub-step E. Switch the five `read_hook_status(&...id)` call sites to `read_hook_status_for_instance(&...)`:
    - `src/tui/app.rs:1055`: replace `crate::hooks::read_hook_status(&instance.id)` with `crate::hooks::read_hook_status_for_instance(&instance)`.
    - `src/server/api/sessions.rs:1095`: replace `crate::hooks::read_hook_status(&decision_instance.id)` with `crate::hooks::read_hook_status_for_instance(&decision_instance)`.
    - `src/cli/session.rs:535`: replace `crate::hooks::read_hook_status(&inst.id)` with `crate::hooks::read_hook_status_for_instance(&inst)`.
    - `src/session/instance.rs:1603`: replace `crate::hooks::read_hook_status(&self.id).is_some()` with `crate::hooks::read_hook_status_for_instance(self).is_some()`.
    - `src/session/instance.rs:1761`: replace `crate::hooks::read_hook_status(&self.id)` with `crate::hooks::read_hook_status_for_instance(self)`.

    Note on fan-out: this task touches seven files. Six of the seven (the five call sites above + `src/hooks/mod.rs` re-export + the two `cleanup_hook_status_dir` call sites in sub-step F) are mechanical edits with no logic content; the substantive code change lives entirely in `src/hooks/status_file.rs`. The fan-out is acceptable because splitting the call-site updates into a separate task would create a wave-2-blocking dependency on an intermediate state where the new shim exists but call sites still use the legacy fn (clippy would flag the unused re-export, and the build would still pass but with dead-code warnings; the small mechanical-edit fan-out keeps the change atomic).

    Sub-step F. Switch the two `cleanup_hook_status_dir` call sites:
    - `src/session/instance.rs:1642`: replace `crate::hooks::cleanup_hook_status_dir(&self.id)` with `crate::hooks::cleanup_hook_status_dir_for_instance(self)`.
    - `src/session/deletion.rs:300`: replace `crate::hooks::cleanup_hook_status_dir(&request.instance.id)` with `crate::hooks::cleanup_hook_status_dir_for_instance(&request.instance)`.

    Sub-step G. Add tests for the new fn AND the pure dispatcher in the existing `#[cfg(test)] mod tests` block at the bottom of `src/hooks/status_file.rs`:

    `test_sbx_hook_status_dir_path`: assert `sbx_hook_status_dir(Path::new("/p/q"), "abc")` equals `PathBuf::from("/p/q/.aoe-hooks/abc")`.

    `test_sbx_hook_status_dir_with_tempdir`: create `tempfile::TempDir`; assert `sbx_hook_status_dir(td.path(), "instance-x")` equals `td.path().join(".aoe-hooks").join("instance-x")`.

    `resolve_status_dir_legacy_when_arbitrary_paths_supported`: assert `resolve_status_dir(true, Path::new("/proj"), "id-x")` equals `hook_status_dir("id-x")`, and that the returned path begins with `/tmp/aoe-hooks/` (the docker/podman branch shape). This pins the legacy dispatch arm.

    `resolve_status_dir_sbx_when_arbitrary_paths_unsupported`: assert `resolve_status_dir(false, Path::new("/proj"), "id-x")` equals `PathBuf::from("/proj/.aoe-hooks/id-x")` (the sbx branch shape under `<project_path>/.aoe-hooks/`). This pins the sbx dispatch arm.

    `test_parse_status_content_running`: assert `parse_status_content("running")` returns `Some(Status::Running)`; same for `waiting`, `idle`. Assert `parse_status_content("garbage")` returns `None`.

    These four tests give the dispatch site automated coverage without requiring a mocking seam for `get_container_runtime()`. The `_for_instance` shims are thin wrappers that read `caps.supports_arbitrary_volume_paths` and pass it to `resolve_status_dir`; the wrappers themselves remain covered by inspection (single `caps = ... .capabilities()` line + the unit-tested `resolve_status_dir` call), which is the smallest mocking-free surface that satisfies VALIDATION.md row `status_dir_dispatch_via_caps`.

    Sub-step H. Verify no `--` separators in doc comments / new prose; verify no dead code (`parse_status_content` is consumed by both `read_hook_status` and `read_hook_status_for_instance`; `resolve_status_dir` is consumed by both `_for_instance` shims AND by the two new unit tests; `sbx_hook_status_dir` is consumed by `resolve_status_dir` AND has a public re-export which Phase 5 may consume directly when wiring `SbxRuntime::create_container`).
  </action>
  <verify>
    <automated>cargo build --features serve 2>&1 | tail -10 && cargo test --lib hooks::status_file 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'pub fn sbx_hook_status_dir' src/hooks/status_file.rs` returns 1.
    - `grep -c 'pub fn resolve_status_dir' src/hooks/status_file.rs` returns 1.
    - `grep -c 'pub fn read_hook_status_for_instance' src/hooks/status_file.rs` returns 1.
    - `grep -c 'pub fn cleanup_hook_status_dir_for_instance' src/hooks/status_file.rs` returns 1.
    - `grep -c 'pub fn hook_status_dir' src/hooks/status_file.rs` returns 1 (existing fn preserved).
    - `grep -c 'pub fn read_hook_status\b' src/hooks/status_file.rs` returns 1 (existing fn preserved; no rename).
    - `grep -c 'pub fn cleanup_hook_status_dir\b' src/hooks/status_file.rs` returns 1 (existing fn preserved).
    - `grep -c 'sbx_hook_status_dir' src/hooks/mod.rs` returns at least 1 (re-export).
    - `grep -c 'resolve_status_dir' src/hooks/mod.rs` returns at least 1 (re-export).
    - `grep -rc 'read_hook_status(' src/tui/app.rs src/server/api/sessions.rs src/cli/session.rs` totals 0 (all switched to `_for_instance` form).
    - `grep -rc 'read_hook_status_for_instance' src/tui/app.rs src/server/api/sessions.rs src/cli/session.rs src/session/instance.rs` totals at least 5.
    - `grep -c 'cleanup_hook_status_dir_for_instance' src/session/instance.rs` returns at least 1; same for `src/session/deletion.rs`.
    - `grep -c 'crate::hooks::cleanup_hook_status_dir(&' src/session/instance.rs src/session/deletion.rs` returns 0 (legacy bare calls switched).
    - `cargo build --features serve` and `cargo build` (TUI-only) both exit 0.
    - `cargo test --lib hooks::status_file::tests::test_sbx_hook_status_dir_path` exits 0.
    - `cargo test --lib hooks::status_file::tests::test_sbx_hook_status_dir_with_tempdir` exits 0.
    - `cargo test --lib hooks::status_file::tests::resolve_status_dir_legacy_when_arbitrary_paths_supported` exits 0.
    - `cargo test --lib hooks::status_file::tests::resolve_status_dir_sbx_when_arbitrary_paths_unsupported` exits 0.
    - `cargo test --lib hooks::status_file::tests::test_parse_status_content_running` exits 0.
    - `cargo test --lib hooks::status_file` exits 0 with all existing tests + 5 new ones passing.
    - `cargo clippy --features serve -- -D warnings` is clean (call-site changes touch hot paths; no new clippy warnings).
    - `grep -nE '— | --[^-]' src/hooks/status_file.rs | grep -vE '"(--[a-z-]+)"' | wc -l` returns 0 (no em-dash or `--` separators in human-authored prose; CLI flag literals are exempt).
  </acceptance_criteria>
  <done>
    `sbx_hook_status_dir`, `resolve_status_dir`, and the two `_for_instance` dispatch shims exist in `src/hooks/status_file.rs`. The pure `resolve_status_dir` dispatcher has two dedicated unit tests pinning both branches of the capability flag (VALIDATION.md row `status_dir_dispatch_via_caps` satisfied with automated tests, not "Phase 5 will revisit"). All five `read_hook_status` call sites and both `cleanup_hook_status_dir` call sites consume the new shims. Builds pass under both feature configurations. New tests pass; existing tests untouched.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add cargo xtask check-kit + fixture-driven integration test + CI job</name>
  <files>
    xtask/src/main.rs,
    xtask/Cargo.toml,
    .github/workflows/ci.yml,
    tests/xtask_check_kit.rs,
    tests/fixtures/sbx_kits/ok/spec.yaml,
    tests/fixtures/sbx_kits/missing-schema-version/spec.yaml,
    tests/fixtures/sbx_kits/naked-install-verb/spec.yaml,
    tests/fixtures/sbx_kits/guarded-install-verb/spec.yaml
  </files>
  <read_first>
    xtask/src/main.rs,
    xtask/Cargo.toml,
    .github/workflows/ci.yml,
    src/containers/sbx_kit/spec.yaml,
    .planning/phases/04-embedded-sbx-kit-and-materialization/04-RESEARCH.md
  </read_first>
  <behavior>
    - `cargo xtask check-kit` exits 0 when run against the project's embedded `src/containers/sbx_kit/spec.yaml` (the one authored in 04-01).
    - `cargo xtask check-kit --spec-path tests/fixtures/sbx_kits/ok/spec.yaml` exits 0.
    - `cargo xtask check-kit --spec-path tests/fixtures/sbx_kits/missing-schema-version/spec.yaml` exits non-zero; stderr contains `schemaVersion`.
    - `cargo xtask check-kit --spec-path tests/fixtures/sbx_kits/naked-install-verb/spec.yaml` exits non-zero; stderr contains `naked install verb`.
    - `cargo xtask check-kit --spec-path tests/fixtures/sbx_kits/guarded-install-verb/spec.yaml` exits 0.
    - `.github/workflows/ci.yml` declares a top-level `kit:` job that runs `cargo xtask check-kit` (no path filter; every push, mirrors `skill:`).
    - `cargo test --test xtask_check_kit` exits 0 with the four fixture-driven cases all passing.
  </behavior>
  <action>
    Sub-step A. Update `xtask/Cargo.toml`. The existing file may already pick up `serde_yaml` transitively, but explicit is safer. Read the existing `xtask/Cargo.toml` first; if `serde_yaml` is NOT listed, add `serde_yaml = "0.9"` and `clap = { version = "4.6", features = ["derive"] }` (clap is already a dep of xtask per the existing `use clap::...` import; confirm). Do NOT add new external deps beyond what's needed for the check-kit body (no `regex` if substring scans suffice; the parser uses `serde_yaml::Value` indexing only).

    Sub-step B. Edit `xtask/src/main.rs`:

    1. Extend the `Commands` enum with a new variant:
       ```
       /// Validate the embedded sbx kit spec.yaml is well-formed and idempotency-guarded
       CheckKit {
           /// Optional path to a spec.yaml fixture (defaults to the project's embedded src/containers/sbx_kit/spec.yaml)
           #[arg(long)]
           spec_path: Option<std::path::PathBuf>,
       },
       ```

    2. Extend the `match args.command` arm in `main()`:
       ```
       Commands::CheckKit { spec_path } => check_kit(spec_path.as_deref()),
       ```

    3. Implement `fn check_kit(spec_path: Option<&std::path::Path>)`:
       - When `spec_path` is `Some(p)`, parse the YAML by reading `p` via `fs::read_to_string`. When `None`, parse the embedded source via `include_str!("../../src/containers/sbx_kit/spec.yaml")` (the path is relative to `xtask/src/main.rs`; confirm with one `cargo build -p xtask` after creation).
       - Parse via `serde_yaml::from_str::<serde_yaml::Value>`. On parse error: `eprintln!("check-kit: failed to parse spec.yaml: {}", e); std::process::exit(1);`.
       - Accumulate `let mut has_error = false;` and call `eprintln!` for each violation (mirror `check_skill` at xtask/src/main.rs:68-86):
         - Assert `spec["schemaVersion"].as_str() == Some("1")` (NOT the int `1`; the YAML must have it as a string per RESEARCH key implementation specifics + Pitfall 2). On miss: `eprintln!("check-kit: schemaVersion must be the string \"1\""); has_error = true;`.
         - Assert `spec["kind"].as_str().is_some()`. On miss: error message names the missing field.
         - Assert `spec["name"].as_str().is_some()`. On miss: same.
         - Assert `spec["commands"]["install"].as_sequence().is_some()`. On miss: same.
         - Assert `spec["commands"]["startup"].as_sequence().is_some()` (may be an empty array per RESEARCH but the key MUST exist). On miss: same.
       - For each `cmd` in `spec["commands"]["startup"].as_sequence()`: extract the command string via the helper `extract_command_string(&serde_yaml::Value) -> Option<String>` that handles both `command: "string"` and `command: ["sh","-c","..."]` array forms (RESEARCH key implementation specific #2 / Example 6). For array form where `command[0..2] == ["sh","-c"]`, return `command[2..].join(" ")`. Else return `command[0..].join(" ")`. Then call `lint_command_for_naked_install_verbs(&s, &mut has_error)`.
       - Naked install verbs: `&["apt-get install", "apt install", "apt-get update", "apt update", "dpkg -i", "dpkg --install", "pip install", "pip3 install", "pip2 install", "npm install -g", "npm i -g", "yarn global add", "yarn add", "curl ", "wget ", "gem install"]`.
       - Idempotency guard prefixes (must appear `cmd.trim_start().starts_with(p)` OR `cmd.contains(" || ")` OR `cmd.contains("&& ")`): `&["[ -f", "[ -x", "[ -e", "[ -d", "test -f", "test -x", "test -e", "test -d", "command -v", "which "]`.
       - For each verb that appears in the command string with no guard, emit `eprintln!("check-kit: naked install verb `{}` in commands.startup without idempotency guard:\n  {}", verb, command_string); has_error = true;`.
       - Apply the same lint to `spec["commands"]["install"]` too; the project's spec.yaml (04-01) has TWO `commands.install` entries: the kit-bootstrap exec and the `.gitignore` append. The append entry's `command` MUST contain the `grep -qsE` idempotency guard for `.aoe-hooks/`. Don't conflate the two passes; loop over both `install` and `startup` arrays with the same lint and let the guard detection do its work.
       - At the end: `if has_error { std::process::exit(1); } println!("check-kit: OK");`.

    Sub-step C. Fixture spec.yaml files in `tests/fixtures/sbx_kits/`:

    `ok/spec.yaml`:
    ```
    schemaVersion: "1"
    kind: agent
    name: ok-agent
    commands:
      install:
        - command: ["sh", "-c", "true"]
      startup:
        - command: ["sh", "-c", "command -v claude >/dev/null || true"]
    ```

    `missing-schema-version/spec.yaml`:
    ```
    kind: agent
    name: missing-schema
    commands:
      install:
        - command: ["sh", "-c", "true"]
      startup: []
    ```

    `naked-install-verb/spec.yaml`:
    ```
    schemaVersion: "1"
    kind: agent
    name: naked-verb
    commands:
      install:
        - command: ["sh", "-c", "true"]
      startup:
        - command: ["sh", "-c", "apt-get install -y curl"]
    ```

    `guarded-install-verb/spec.yaml`:
    ```
    schemaVersion: "1"
    kind: agent
    name: guarded-verb
    commands:
      install:
        - command: ["sh", "-c", "true"]
      startup:
        - command: ["sh", "-c", "command -v curl >/dev/null || apt-get install -y curl"]
    ```

    Sub-step D. Integration test `tests/xtask_check_kit.rs`. Use `std::process::Command` to spawn `cargo run -p xtask -- check-kit --spec-path <fixture>`. The pattern is:
    ```rust
    fn run_check_kit(fixture: &str) -> std::process::Output {
        std::process::Command::new("cargo")
            .args(["run", "-q", "-p", "xtask", "--", "check-kit", "--spec-path", fixture])
            .output()
            .expect("xtask invocation failed")
    }

    #[test]
    fn ok_fixture_passes() {
        let out = run_check_kit("tests/fixtures/sbx_kits/ok/spec.yaml");
        assert!(out.status.success(), "stderr: {}", String::from_utf8_lossy(&out.stderr));
    }

    #[test]
    fn missing_schema_version_fails() {
        let out = run_check_kit("tests/fixtures/sbx_kits/missing-schema-version/spec.yaml");
        assert!(!out.status.success());
        assert!(String::from_utf8_lossy(&out.stderr).contains("schemaVersion"));
    }

    #[test]
    fn naked_install_verb_fails() {
        let out = run_check_kit("tests/fixtures/sbx_kits/naked-install-verb/spec.yaml");
        assert!(!out.status.success());
        assert!(String::from_utf8_lossy(&out.stderr).contains("naked install verb"));
    }

    #[test]
    fn guarded_install_verb_passes() {
        let out = run_check_kit("tests/fixtures/sbx_kits/guarded-install-verb/spec.yaml");
        assert!(out.status.success(), "stderr: {}", String::from_utf8_lossy(&out.stderr));
    }

    #[test]
    fn embedded_spec_passes() {
        // The project's own spec.yaml (authored in 04-01) must satisfy the lint.
        let out = std::process::Command::new("cargo")
            .args(["run", "-q", "-p", "xtask", "--", "check-kit"])
            .output()
            .expect("xtask invocation failed");
        assert!(out.status.success(), "stderr: {}", String::from_utf8_lossy(&out.stderr));
    }
    ```

    Sub-step E. `.github/workflows/ci.yml`. After the existing `skill:` job (lines 82-93), add a peer `kit:` job mirroring its structure:
    ```yaml
      kit:
        name: Kit Check
        runs-on: ubuntu-latest
        steps:
          - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
          - uses: dtolnay/rust-toolchain@stable
          - uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4 # v2.9.1
            with:
              cache-bin: "false"
              save-if: ${{ github.ref == 'refs/heads/main' }}
          - name: Validate embedded sbx kit spec.yaml
            run: cargo xtask check-kit
    ```
    Action SHAs are pinned identically (NEVER use floating tags). The job is independent of `skill:` and runs in parallel.

    Sub-step F. Verify the embedded spec.yaml from 04-01 passes the check. If 04-01's spec.yaml fails the lint (e.g., the startup command contains an unguarded substring), the executor MUST fix the embedded spec.yaml at `src/containers/sbx_kit/spec.yaml` per the 04-01 acceptance criteria (the spec MUST be self-consistent). The expected guard pattern in 04-01's spec.yaml is `command -v <agent> >/dev/null 2>&1 || ...` for startup and `grep -qsE ... || ...` for the install append; both satisfy the lint.

    Sub-step G. Per AGENTS.md: no em-dashes / `--` separators in xtask doc comments or println! strings. The `--spec-path` CLI flag is a clap arg name and is unavoidable; it is an arg flag, not a prose separator. Document this exemption in a single line comment above the `#[arg(long)]` if reviewer-confusion is a concern.
  </action>
  <verify>
    <automated>cargo build -p xtask 2>&1 | tail -10 && cargo xtask check-kit 2>&1 | tail -5 && cargo test --test xtask_check_kit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `xtask/src/main.rs` contains a `CheckKit` variant in `Commands` (verify via `grep -c 'CheckKit' xtask/src/main.rs` returns at least 2; variant + match arm).
    - `xtask/src/main.rs` contains `fn check_kit(` (verify via `grep -c 'fn check_kit\b' xtask/src/main.rs` returns 1).
    - `cargo build -p xtask` exits 0.
    - `cargo xtask check-kit` (no args) exits 0 against the embedded src/containers/sbx_kit/spec.yaml from 04-01.
    - `cargo xtask check-kit --spec-path tests/fixtures/sbx_kits/missing-schema-version/spec.yaml` exits 1; stderr contains `schemaVersion`.
    - `cargo xtask check-kit --spec-path tests/fixtures/sbx_kits/naked-install-verb/spec.yaml` exits 1; stderr contains `naked install verb`.
    - `cargo xtask check-kit --spec-path tests/fixtures/sbx_kits/guarded-install-verb/spec.yaml` exits 0.
    - `cargo test --test xtask_check_kit` exits 0 with five passing tests (the four fixtures + `embedded_spec_passes`).
    - `.github/workflows/ci.yml` contains a `kit:` job with `run: cargo xtask check-kit` (verify via `grep -c 'cargo xtask check-kit' .github/workflows/ci.yml` returns at least 1; `grep -c '  kit:' .github/workflows/ci.yml` returns 1 for the top-level job key).
    - The new CI job uses the same pinned action SHAs as `skill:` (verify via `grep -c 'de0fac2e4500dabe0009e67214ff5f5447ce83dd' .github/workflows/ci.yml` increased by 1 vs. pre-04-02).
    - All four fixture spec.yaml files exist under `tests/fixtures/sbx_kits/<name>/spec.yaml`.
    - `cargo clippy -p xtask -- -D warnings` is clean.
    - `cargo fmt --all -- --check` passes.
  </acceptance_criteria>
  <done>
    `cargo xtask check-kit` is a real subcommand that lint-validates `src/containers/sbx_kit/spec.yaml`. Integration test `tests/xtask_check_kit.rs` covers all four fixture cases plus the embedded spec. CI runs the check on every push as a peer `kit:` job. No new external deps beyond what was already in xtask/Cargo.toml or what serde_yaml requires.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sbx microVM workspace, host poller | Host poller reads `<project_path>/.aoe-hooks/<instance_id>/status` written by the in-VM agent (uid 1000). The workspace mount crosses the trust boundary. |
| call-site `Instance`, dispatch shim | New `_for_instance` shims accept `&Instance`; `instance.project_path` and `instance.id` flow into `PathBuf::join`. |
| xtask subcommand, CI | `cargo xtask check-kit` runs against the embedded spec.yaml on every push and against fixture files for tests. |
| spec.yaml authoring, install-verb lint | The lint enforces idempotency guards on `commands.{install,startup}`; pre-merge gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-02-01 | Tampering | `read_hook_status_for_instance` reading `<project_path>/.aoe-hooks/<id>/status` | low | accept | The agent writes its own status as the agent user inside the VM; host poller reads as the user. An attacker who can write `<project_path>/.aoe-hooks/<id>/status` already has write access to the user's workspace (the same workspace the agent edits). No elevation here. Documenting as accepted. |
| T-04-02-02 | Tampering | `sbx_hook_status_dir(&Path, &str)` path injection | medium | mitigate | `project_path` originates from `Instance.project_path` (set at session-create time, validated by Phase 3's `compute_volume_paths`). `instance_id` is a UUID-shaped string generated by aoe (`uuid` crate). Both inputs are bounded. **Mitigation**: trust the existing validation; no new checks. If a future change makes `instance.id` user-controllable, the dispatch shim is the chokepoint where an `id` validator can be inserted. Acceptance: documented invariant in the dispatch shim doc-comment. |
| T-04-02-03 | Tampering | `cleanup_hook_status_dir_for_instance`, `remove_dir_all` | medium | mitigate | Deletes `<project_path>/.aoe-hooks/<instance_id>/`. If `instance_id` contained `..`, the delete could escape into the workspace root. **Mitigation**: instance ids are UUID-shaped (`uuid::Uuid::new_v4().to_string()` per `Instance::new` at instance.rs:446); the pattern `[a-f0-9-]+` excludes `..`. Acceptance: existing `Instance::new` is the gate; no new code. Future-flag: a fuzz test inserting `../etc` would catch a regression. |
| T-04-02-04 | Denial of Service | `cargo xtask check-kit` runtime | low | accept | The xtask invokes `serde_yaml::from_str` + substring scans on a single embedded file (< 10 KB). Sub-second runtime; no DoS concern at CI scale. |
| T-04-02-05 | Tampering | Install-verb lint bypass | medium | mitigate | An author could write `apt-get install` with a clever-but-incorrect guard prefix. **Mitigation**: lint accepts a small fixed set of guard prefixes (`[ -f`, `command -v`, `||`, `&&`); novel patterns fall through to "naked verb" and fail the lint. Acceptance: `guarded-install-verb` fixture passes; `naked-install-verb` fixture fails. Edge case where an author writes a custom guard not in the prefix list: by design the lint errs on the strict side and forces the author to use one of the recognized guards. |
| T-04-02-06 | Tampering | Embedded spec.yaml path resolution in xtask | low | accept | `include_str!("../../src/containers/sbx_kit/spec.yaml")` resolves at compile time relative to `xtask/src/main.rs`. A directory rename would surface as a build error, not a runtime escape. Accepted as compile-time safety. |
| T-04-02-07 | Tampering | Supply chain (no new packages) | low | accept | Phase 4-02 adds zero new external dependencies. `serde_yaml` and `clap` were already in xtask/Cargo.toml (or pulled transitively); the new test crate uses only stdlib + cargo-built xtask. No `[ASSUMED]` / `[SUS]` packages. No legitimacy checkpoint required. |
</threat_model>

<verification>
- `cargo build --features serve` and `cargo build` (TUI-only) both pass.
- `cargo test --lib hooks::status_file` passes with the 5 new tests + existing 11.
- `cargo test --test xtask_check_kit` passes (5 tests).
- `cargo xtask check-kit` exits 0 against the embedded spec.yaml.
- `cargo clippy --features serve -- -D warnings` passes; `cargo clippy -p xtask -- -D warnings` passes.
- `cargo fmt --all -- --check` passes.
- `grep -c 'cargo xtask check-kit' .github/workflows/ci.yml` returns at least 1.
- Manual: open the diff for `src/tui/app.rs`, `src/server/api/sessions.rs`, `src/cli/session.rs`, `src/session/instance.rs`, `src/session/deletion.rs`; confirm each `read_hook_status(...)` / `cleanup_hook_status_dir(...)` call has been replaced (no leftover legacy call to those names).
</verification>

<success_criteria>
- KIT-04 fully satisfied (combined with 04-01's in-VM half): host side reads `<project_path>/.aoe-hooks/<id>/status` for sbx-runtime instances, `/tmp/aoe-hooks/<id>/status` for Docker/Podman/AppleContainer instances; dispatch lives at one site (`src/hooks/status_file.rs::resolve_status_dir`), unit-tested in both branches; all call sites consume the new shim.
- KIT-06 satisfied: `cargo xtask check-kit` parses the embedded spec.yaml directly, asserts `schemaVersion == "1"`, required fields present, install-verb lint with idempotency-guard recognition; CI runs the check as a peer job to `skill:`.
- All four fixture cases (`ok`, `missing-schema-version`, `naked-install-verb`, `guarded-install-verb`) plus the embedded spec.yaml have automated regression coverage in `tests/xtask_check_kit.rs`.
- No file owned by 04-01 (`src/containers/sbx/kit.rs`, `src/containers/sbx_kit/*`) is modified here; wave parallelism contract honored.
</success_criteria>

<output>
Create `.planning/phases/04-embedded-sbx-kit-and-materialization/04-02-SUMMARY.md` when done, following the standard summary template.
</output>
