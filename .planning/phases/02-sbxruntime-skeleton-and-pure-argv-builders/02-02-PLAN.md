---
phase: 02-sbxruntime-skeleton-and-pure-argv-builders
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/containers/runtime.rs
autonomous: true
requirements:
  - PHASE-2-SCAFFOLDING
  - SC-4

must_haves:
  truths:
    - "SC-4: ContainerRuntime built via ::sbx() routes is_available() / exec_command() / build_create_args() to SbxRuntime (peer struct) and sbx::argv (free functions). The other RuntimeKind::Sbx dispatch arms (does_container_exist, is_container_running, batch_running_states) remain as Phase 1 stubs unchanged per D-05."
    - "SC-4 (no regression): All existing Docker/Podman/AppleContainer dispatch tests in runtime.rs continue to pass unmodified after the three-arm edit."
    - "SC-4 (strengthened sbx test): runtime.rs::tests proves the new builder shape — `build_create_args` returns sbx-shaped argv (`create shell --name ...`), not Docker-shaped argv (`run -d --name ...`); `exec_command` returns a string with `exec` and the cmd token, not the Phase 1 sentinel `/* Phase 1 stub` marker."
  artifacts:
    - path: "src/containers/runtime.rs"
      provides: "Updated ContainerRuntime struct (gains `sbx: Option<sbx::SbxRuntime>` per D-03 Option A), updated four constructors (docker/podman/apple_container set None, sbx() sets Some), three updated dispatch arms (is_available, exec_command, build_create_args), strengthened sbx-dispatch test asserting new builder shape"
      contains: "sbx: Option"
  key_links:
    - from: "src/containers/runtime.rs::ContainerRuntime::is_available"
      to: "src/containers/sbx/mod.rs::SbxRuntime::is_available"
      via: "self.sbx.as_ref().expect(...).is_available()"
      pattern: "RuntimeKind::Sbx.*self\\.sbx.*is_available"
    - from: "src/containers/runtime.rs::ContainerRuntime::build_create_args"
      to: "src/containers/sbx/argv.rs::build_create_args"
      via: "sbx::argv::build_create_args(name, image, None, config)"
      pattern: "sbx::argv::build_create_args"
    - from: "src/containers/runtime.rs::ContainerRuntime::exec_command"
      to: "src/containers/sbx/argv.rs::build_exec_args"
      via: "adapter at dispatch site converts options/cmd into builder args, then joins with `sbx` prefix"
      pattern: "sbx::argv::build_exec_args"

risks:
  - id: R-01
    description: "Roadmap SC-4 wording mentions `RuntimeHandle::Sbx(SbxRuntime)` variant. This is stale from before the facade decision (D-01) was locked. The actual mechanism is the existing `RuntimeKind::Sbx` arms in runtime.rs, which Phase 2 updates per D-04. Do NOT add an enum named `RuntimeHandle` — that would contradict D-01. Plan-checker: read 02-CONTEXT.md D-01 and 02-RESEARCH.md `<phase_requirements>` SC-4 caveat before interpreting SC-4 literally."
  - id: R-02
    description: "The `.expect(...)` panic message on `self.sbx.as_ref()` only fires if `ContainerRuntime::sbx()` is misused (e.g., someone constructs `ContainerRuntime { base: SBX, kind: Sbx, sbx: None }` directly). All four constructors stay in this file; the invariant is local. If a future Phase needs to construct ContainerRuntime from arbitrary fields, the invariant gate must move with it."
---

<objective>
Update `src/containers/runtime.rs` to route the three `RuntimeKind::Sbx` dispatch arms (`is_available`, `exec_command`, `build_create_args`) through the new `src/containers/sbx/` module created in plan 02-01. Add an `sbx: Option<SbxRuntime>` field to `ContainerRuntime` (D-03 Option A — RESEARCH.md recommended for smallest diff). Update all four constructors (`docker`, `apple_container`, `podman`, `sbx`) to initialize the field. Strengthen the existing sbx dispatch test to assert the new builder shape rather than the Phase 1 sentinel string. Existing Docker / Podman / AppleContainer behavior is unchanged.

Purpose: Phase 2's last gate (Roadmap SC-4) lives here — proving the existing facade dispatch picks up the new peer struct without churning any of the ~12 caller sites of `get_container_runtime()`.

Output: 1 modified file (`src/containers/runtime.rs`) with: 1 new struct field; 4 constructor updates (1 line each); 3 dispatch-arm body rewrites; 1 strengthened sbx test plus 1 new sbx test for build_create_args. All existing per-backend capability and dispatch tests continue to pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-sbxruntime-skeleton-and-pure-argv-builders/02-CONTEXT.md
@.planning/phases/02-sbxruntime-skeleton-and-pure-argv-builders/02-RESEARCH.md
@.planning/phases/02-sbxruntime-skeleton-and-pure-argv-builders/02-PATTERNS.md
@.planning/phases/02-sbxruntime-skeleton-and-pure-argv-builders/02-VALIDATION.md
@.planning/phases/02-sbxruntime-skeleton-and-pure-argv-builders/02-01-PLAN.md
@.planning/phases/01-capability-surface-and-trait-foundation/01-CONTEXT.md
@AGENTS.md
@src/containers/runtime.rs
@src/containers/runtime_base.rs
@src/containers/container_interface.rs
@src/containers/mod.rs

<interfaces>
Key shapes the executor needs. Extracted from the codebase.

Current ContainerRuntime struct (src/containers/runtime.rs:22-25):

```rust
pub struct ContainerRuntime {
    pub(crate) base: RuntimeBase,
    pub(crate) kind: RuntimeKind,
}
```

Current four constructors (src/containers/runtime.rs:28-58):

```rust
pub fn docker() -> Self {
    Self { base: RuntimeBase::DOCKER, kind: RuntimeKind::Docker }
}
pub fn apple_container() -> Self {
    Self { base: RuntimeBase::APPLE_CONTAINER, kind: RuntimeKind::AppleContainer }
}
pub fn podman() -> Self {
    Self { base: RuntimeBase::PODMAN, kind: RuntimeKind::Podman }
}
pub fn sbx() -> Self {
    Self { base: RuntimeBase::SBX, kind: RuntimeKind::Sbx }
}
```

Current three arms to update (src/containers/runtime.rs):

```rust
// Arm 1 — is_available (lines 74-79)
fn is_available(&self) -> bool {
    match self.kind {
        RuntimeKind::Sbx => false,
        _ => self.base.is_available(),
    }
}

// Arm 2 — build_create_args (lines 181-183)
fn build_create_args(&self, name: &str, image: &str, config: &ContainerConfig) -> Vec<String> {
    self.base.build_create_args(name, image, config)
}

// Arm 3 — exec_command Sbx arm (lines 240-254, inside the match in fn exec_command)
RuntimeKind::Sbx => {
    let _ = options;
    let _ = cmd;
    format!("sbx exec {} /* Phase 1 stub: cmd and options dropped */", name)
}
```

Existing sbx dispatch test (src/containers/runtime.rs:534-542):

```rust
#[test]
fn test_sbx_dispatch_arms_return_safe_stubs() {
    let rt = ContainerRuntime::sbx();
    assert!(!rt.does_container_exist("foo").unwrap());
    assert!(!rt.is_container_running("foo").unwrap());
    assert!(rt.batch_running_states("aoe-sandbox-").is_empty());
    let cmd = rt.exec_command("foo", None, "bar");
    assert!(cmd.contains("sbx"));
    assert!(cmd.contains("foo"));
}
```

Existing sbx capability-matrix test that must continue to pass unchanged (src/containers/runtime.rs:514-525):

```rust
#[test]
fn test_sbx_capability_matrix() {
    let rt = ContainerRuntime::sbx();
    let caps = rt.capabilities();
    assert!(!caps.supports_read_only_volumes);
    // ...etc, all 7 flags
    assert!(caps.supports_dynamic_port_publish);
}
```

SbxRuntime peer struct from plan 02-01 (src/containers/sbx/mod.rs):

```rust
pub(crate) struct SbxRuntime {
    pub(crate) binary: PathBuf,
}

impl SbxRuntime {
    pub fn new() -> Self { ... }
    pub fn is_available(&self) -> bool { ... }
    pub fn capabilities(&self) -> RuntimeCapabilities { ... }
}
```

Free-function builders from plan 02-01 (src/containers/sbx/argv.rs):

```rust
pub fn build_create_args(name: &str, image: &str, kit_path: Option<&Path>, config: &ContainerConfig) -> Vec<String>
pub fn build_exec_args(name: &str, workdir: Option<&str>, env: &[EnvEntry], interactive: bool, tty: bool, cmd: &[&str]) -> Vec<String>
pub fn build_ports_args(name: &str, port_spec: &str) -> Vec<String>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add sbx storage to ContainerRuntime + update all four constructors + update three RuntimeKind::Sbx dispatch arms</name>
  <files>src/containers/runtime.rs</files>
  <read_first>
    - src/containers/runtime.rs (CURRENT STATE of struct definition at lines 22-25, constructors at lines 28-58, `is_available` arm at lines 74-79, `build_create_args` at lines 181-183, `exec_command` match block at lines 209-256, sbx test at lines 534-542; needed to make precise edits without rewriting the file)
    - src/containers/sbx/mod.rs (from plan 02-01 — confirms `pub(crate) struct SbxRuntime { pub(crate) binary: PathBuf }`, `SbxRuntime::new()`, `SbxRuntime::is_available(&self) -> bool`)
    - src/containers/sbx/argv.rs (from plan 02-01 — confirms three free-function signatures)
    - src/containers/container_interface.rs (lines 5-50: `VolumeMount` / `EnvEntry` / `ContainerConfig` shapes for the new test)
    - .planning/phases/02-sbxruntime-skeleton-and-pure-argv-builders/02-CONTEXT.md (D-01 facade dispatch, D-03 storage shape — Option A recommended, D-04 three-arm minimum surface, D-05 stub arms stay, D-06 action verbs deferred)
    - .planning/phases/02-sbxruntime-skeleton-and-pure-argv-builders/02-RESEARCH.md (storage shape Option A recommendation; SC-4 stale-wording caveat; Wave 0 gaps section)
    - .planning/phases/02-sbxruntime-skeleton-and-pure-argv-builders/02-PATTERNS.md (lines 318-499: runtime.rs section with before/after diff blocks for all three arms and the strengthened test)
    - AGENTS.md ("No dead code"; "No emdashes"; "Don't preserve backwards compatibility by default; call it out when a change is breaking" — the new struct field is a breaking change to external direct-constructors, but ContainerRuntime is private/`pub(crate)`-style: all four constructors stay in this file)
  </read_first>
  <behavior>
    The test surface is the contract that proves SC-4. Add and modify tests in `src/containers/runtime.rs::tests`:

    Modified test (strengthen the existing `test_sbx_dispatch_arms_return_safe_stubs`):
    - Keep the three existing safe-stub assertions (does_container_exist, is_container_running, batch_running_states).
    - Replace the `cmd.contains("sbx")` check (too loose; the Phase 1 sentinel also contained "sbx") with a stricter set. Note: the Phase 1 sentinel was `"sbx exec {} /* Phase 1 stub: cmd and options dropped */"`, so a bare `contains("exec")` is also non-discriminating — the proof of the rewire rests on the absence of the sentinel comment and the presence of the dropped `cmd` token.
      - `assert!(cmd.starts_with("sbx exec "))` — proves new `build_exec_args` ran (the Phase 1 sentinel started with `"sbx exec {}"`, never `"sbx exec "` followed by real argv; the new builder emits real argv tokens immediately after `"sbx exec "`).
      - `assert!(cmd.contains("foo"))` — name preserved.
      - `assert!(cmd.contains("bar"))` — cmd is no longer dropped (Phase 1 ignored it).
      - `assert!(!cmd.contains("/* Phase 1"))` — sentinel comment is gone (discriminating: only the Phase 1 sentinel had this exact prefix).
      - `assert!(!cmd.contains("dropped"))` — sentinel marker is gone.

    New test (`test_sbx_build_create_args_emits_sbx_shape_not_docker_shape`):
    - Construct a `ContainerRuntime::sbx()` and an empty-ish `ContainerConfig` (working_dir="/workspace", all Vec fields empty, cpu_limit=None, memory_limit=None).
    - Call `rt.build_create_args("aoe-sandbox-test", "alpine:latest", &cfg)`.
    - Assert `args[0] == "create"` and `args[1] == "shell"` (sbx shape).
    - Assert `!args.contains(&"run".to_string())` (NOT Docker shape).
    - Assert `!args.contains(&"-d".to_string())` (NOT Docker daemon-mode flag).
    - Assert `args.contains(&"--template".to_string())` (sbx-specific flag).
    - Assert `args.contains(&"alpine:latest".to_string())` (image emitted as `--template <image>`).

    No new constructor tests beyond the existing `test_sbx_runtime_uses_sbx_binary` at runtime.rs:493-498 and `test_get_container_runtime_constructs_sbx_when_kind_matches` at mod.rs:177-181. Those already pass and must continue to pass (the struct field addition is invisible to them).

    All existing non-sbx tests (test_docker_capability_matrix, test_podman_capability_matrix, test_apple_container_capability_matrix, test_podman_runtime_uses_podman_binary, test_podman_supports_docker_compatible_features, test_podman_exec_command_format_matches_docker, test_capabilities_method_routes_through_base, test_sbx_capability_matrix, test_sbx_capabilities_via_trait_method) must continue to pass. NOTE: `test_sbx_is_available_returns_false_unconditionally` at runtime.rs:504-508 asserts `is_available()` returns false unconditionally — that is the Phase 1 contract. Phase 2 replaces this with a real PATH probe (the new `is_available_*` tests in plan 02-01 cover SC-3 via the D-10 injection seam). DELETE this test as part of this task; do not replace. See `<action>` step 7.
  </behavior>
  <action>
    Edit `src/containers/runtime.rs` in place. Make exactly these structural changes — do not refactor unrelated code.

    1. Add module import at the top (after the existing `use super::runtime_base::RuntimeBase;` at line 12): `use super::sbx;`. This brings the `sbx` module from `src/containers/sbx/` into scope (declared in `src/containers/mod.rs` by plan 02-01).

    2. Modify the `ContainerRuntime` struct at lines 22-25 by adding one field. The new shape:
       - `pub(crate) base: RuntimeBase,`
       - `pub(crate) kind: RuntimeKind,`
       - `pub(crate) sbx: Option<sbx::SbxRuntime>,` (new; `Some` only when `kind == Sbx`)

    3. Update all four constructors at lines 28-58:
       - `ContainerRuntime::docker()`, `ContainerRuntime::apple_container()`, `ContainerRuntime::podman()`: each gains `sbx: None,` as a third field in the struct literal.
       - `ContainerRuntime::sbx()`: gains `sbx: Some(sbx::SbxRuntime::new()),` as a third field, and the existing Phase 1 stub comment (lines 49-52) can be tightened — the line "Phase 1 stub; pairs RuntimeBase::SBX with RuntimeKind::Sbx so every dispatch site reaches a safe-stub arm" is stale. Replace it with a one-line comment explaining the field invariant: `// kind == Sbx implies sbx is Some; the three updated dispatch arms unwrap via .expect("kind=Sbx invariant").`

    4. Rewrite the `is_available` arm at lines 74-79. Before: `RuntimeKind::Sbx => false,`. After:
       - `RuntimeKind::Sbx => self.sbx.as_ref().expect("ContainerRuntime::sbx() invariant: sbx field must be Some when kind == Sbx").is_available(),`
       - The `_ => self.base.is_available(),` fall-through stays unchanged.

    5. Rewrite the `build_create_args` method at lines 181-183. Before: unconditional `self.base.build_create_args(name, image, config)`. After: introduce a `match self.kind` that routes `RuntimeKind::Sbx` to `sbx::argv::build_create_args(name, image, None, config)` and falls through with `_ => self.base.build_create_args(name, image, config)`. The `None` is the deferred kit_path (D-08; Phase 4 plumbs `Some(materialized_path)` later).

    6. Rewrite the `RuntimeKind::Sbx` arm of `exec_command` at lines 240-254. Replace the entire arm body with an adapter that calls `sbx::argv::build_exec_args` and joins the result with the "sbx" command prefix:
       - The `options: Option<&str>` parameter currently encodes a docker-style passthrough (e.g. "-w /workspace"). For Phase 2, the adapter handles the common case: if `options` starts with `"-w "`, extract the workdir; otherwise pass `None`.
       - `env: &[EnvEntry]` parameter is not available at this call site (the trait method signature is fixed by `container_interface.rs:130`). Pass `&[]` for now; Phase 5 introduces real env wiring at a higher layer.
       - `interactive: true, tty: true` (matches Docker / Podman default at this dispatch site; the parent `exec_command` for Docker emits `-it`).
       - `cmd: &[cmd]` (single-element slice from the `cmd: &str` parameter).
       - Compose the final return string: prepend `"sbx"` to the builder's `Vec<String>` and join with spaces. Use `std::iter::once("sbx".to_string()).chain(args).collect::<Vec<_>>().join(" ")` per PATTERNS.md lines 411-423.

    7. Delete the test `test_sbx_is_available_returns_false_unconditionally` at lines 504-508. The Phase 1 unconditional-false contract is gone; SC-3 coverage now lives in plan 02-01's in-module tests `is_available_with_injected_fake_binary_returns_true` and `is_available_with_missing_binary_returns_false` (both using the D-10 injection seam). A `runtime.rs` parity test that invokes `Command::new("sbx").arg("--version").output()` and asserts `rt.is_available() == that_outcome` would be tautological (both sides compute the same probe against the same binary) and adds no coverage beyond plan 02-01. Drop it; do not replace.

    8. Strengthen `test_sbx_dispatch_arms_return_safe_stubs` at lines 534-542 per `<behavior>`. Keep the test name; replace the loose `cmd.contains("sbx")` assertion with the stricter assertion set listed there.

    9. Add the new test `test_sbx_build_create_args_emits_sbx_shape_not_docker_shape` immediately after `test_sbx_dispatch_arms_return_safe_stubs` per `<behavior>`. It constructs a minimal `ContainerConfig` and asserts the sbx-specific argv shape.

    Do NOT touch any other RuntimeKind::Sbx arms (does_container_exist at 130-134, is_container_running at 171-176, batch_running_states at 303-310). They stay as Phase 1 stubs per D-05/D-06.

    Do NOT introduce a `RuntimeHandle` enum or any other type from the stale SC-4 wording — that contradicts D-01.

    Do NOT add `#[allow(dead_code)]` for any new field or function. The new `sbx` field is read by the three updated dispatch arms.

    After edits: run `cargo fmt`, then `cargo clippy -- -D warnings`. Fix any clippy warnings (most likely candidate: `.expect(...)` lints can be suppressed by accepting their intent — they are documented invariants, not unwraps on user input).
  </action>
  <verify>
    <automated>cargo fmt --check && cargo clippy -- -D warnings && cargo test -p agent-of-empires --lib containers::</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'pub(crate) sbx: Option<sbx::SbxRuntime>' src/containers/runtime.rs` returns 1.
    - `grep -c 'use super::sbx;' src/containers/runtime.rs` returns 1.
    - `grep -c 'sbx: Some(sbx::SbxRuntime::new())' src/containers/runtime.rs` returns 1 (inside `ContainerRuntime::sbx()`).
    - `grep -c 'sbx: None' src/containers/runtime.rs` returns at least 3 (inside `docker()`, `apple_container()`, `podman()` constructors).
    - `grep -c 'sbx::argv::build_create_args' src/containers/runtime.rs` returns 1.
    - `grep -c 'sbx::argv::build_exec_args' src/containers/runtime.rs` returns 1.
    - `grep -c '/\* Phase 1 stub' src/containers/runtime.rs` returns 0 inside the `exec_command` body (the sentinel comment is gone).
    - `grep -c 'self.sbx.as_ref().expect(' src/containers/runtime.rs` returns at least 1.
    - The Phase 1 sbx is_available test is gone or renamed: `grep -c 'test_sbx_is_available_returns_false_unconditionally' src/containers/runtime.rs` returns 0.
    - The new sbx build_create_args test exists: `grep -c 'test_sbx_build_create_args_emits_sbx_shape_not_docker_shape' src/containers/runtime.rs` returns 1.
    - The strengthened sbx dispatch test exists with the new assertions: `grep -c 'assert!(!cmd.contains("/\* Phase 1"))' src/containers/runtime.rs` returns 1 AND `grep -c 'assert!(cmd.starts_with("sbx exec "))' src/containers/runtime.rs` returns 1.
    - `cargo fmt --check` exits 0.
    - `cargo clippy -- -D warnings` exits 0.
    - `cargo test -p agent-of-empires --lib containers::` exits 0.
    - Specifically: `cargo test -p agent-of-empires --lib containers::runtime::tests::test_sbx_build_create_args_emits_sbx_shape_not_docker_shape` exits 0.
    - Specifically: `cargo test -p agent-of-empires --lib containers::runtime::tests::test_sbx_dispatch_arms_return_safe_stubs` exits 0.
    - The unrelated existing tests continue to pass: `cargo test -p agent-of-empires --lib containers::runtime::tests::test_docker_capability_matrix containers::runtime::tests::test_podman_capability_matrix containers::runtime::tests::test_apple_container_capability_matrix containers::runtime::tests::test_capabilities_method_routes_through_base containers::runtime::tests::test_sbx_capability_matrix` all pass (no regression from the struct field addition).
    - No `#[allow(dead_code)]` added: `grep -c 'allow(dead_code)' src/containers/runtime.rs` returns 0.
  </acceptance_criteria>
  <done>`src/containers/runtime.rs` has the `ContainerRuntime` struct extended with `sbx: Option<SbxRuntime>` per D-03 Option A. All four constructors initialize the field correctly. Three `RuntimeKind::Sbx` dispatch arms (`is_available`, `exec_command`, `build_create_args`) route through the new module. The Phase 1 sentinel string in `exec_command` is gone. The Phase 1 unconditional-false `is_available` test is deleted (SC-3 now covered by plan 02-01 via the D-10 injection seam). A new `test_sbx_build_create_args_emits_sbx_shape_not_docker_shape` test proves the new builder shape. The strengthened `test_sbx_dispatch_arms_return_safe_stubs` proves the new `exec_command` shape via discriminating assertions (`starts_with("sbx exec ")` + `!contains("/* Phase 1")`). cargo fmt + cargo clippy + cargo test all green. No regression on Docker / Podman / AppleContainer tests.</done>
</task>

</tasks>

<verification>
After the task completes:

1. SC-4 (no regression on existing runtimes):
   - `cargo test -p agent-of-empires --lib containers::` exits 0.
   - All seven existing per-backend capability tests pass (Docker, Podman, AppleContainer matrices + the trait-routing accessor test).

2. SC-4 (route through):
   - `cargo test -p agent-of-empires --lib containers::runtime::tests::test_sbx_dispatch_arms_return_safe_stubs` exits 0 and the new stricter assertions pass.
   - `cargo test -p agent-of-empires --lib containers::runtime::tests::test_sbx_build_create_args_emits_sbx_shape_not_docker_shape` exits 0.

3. SC-3 (is_available no longer hard-coded false): the deleted Phase 1 unconditional-false test confirms the dispatch arm now routes to the peer struct's real probe. SC-3 coverage is delivered by plan 02-01's `is_available_with_injected_fake_binary_returns_true` and `is_available_with_missing_binary_returns_false` via the D-10 injection seam — those tests construct `SbxRuntime { binary: ... }` directly and assert both true and false outcomes deterministically without depending on the test runner's PATH.

4. Module boundary:
   - `grep -rn 'sbx::argv::build_' src/containers/ --include='*.rs'` shows hits ONLY in `src/containers/runtime.rs` and `src/containers/sbx/argv.rs` (no other call sites — D-04 minimum surface).
   - `grep -rn 'sbx::SbxRuntime' src/containers/ --include='*.rs'` shows hits ONLY in `src/containers/runtime.rs` (the struct field + constructor) and `src/containers/sbx/mod.rs` (the definition).

5. Repo policy:
   - `cargo fmt --check` exits 0.
   - `cargo clippy -- -D warnings` exits 0.
   - `grep -c 'allow(dead_code)' src/containers/runtime.rs` returns 0.

6. ~12 caller sites of `get_container_runtime()` are unchanged (D-01 guarantee). Inspect via:
   - `grep -rn 'get_container_runtime()' src/ --include='*.rs' | wc -l` returns the same count as before this plan (sanity check — value should match git's pre-change state).
</verification>

<success_criteria>
- `src/containers/runtime.rs` compiles with three `RuntimeKind::Sbx` dispatch arms routed through `src/containers/sbx/`.
- `cargo test -p agent-of-empires --lib containers::` passes with no regressions.
- SC-4 (Roadmap, despite stale `RuntimeHandle` wording) is delivered: dispatch routes correctly through the existing `ContainerRuntime` facade to the new peer struct + free functions, and no existing backend (Docker / Podman / AppleContainer) test fails.
- The strengthened `test_sbx_dispatch_arms_return_safe_stubs` proves the new `exec_command` builder ran; the new `test_sbx_build_create_args_emits_sbx_shape_not_docker_shape` proves `build_create_args` no longer falls through to `RuntimeBase`.
- No new `#[allow(dead_code)]`, no new `#[cfg(target_os = ...)]`, no scattered platform gates.
- `cargo fmt --check` and `cargo clippy -- -D warnings` both exit 0.
</success_criteria>

<output>
Create `.planning/phases/02-sbxruntime-skeleton-and-pure-argv-builders/02-02-SUMMARY.md` when done.
</output>
