---
phase: 02-sbxruntime-skeleton-and-pure-argv-builders
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/containers/sbx/mod.rs
  - src/containers/sbx/argv.rs
  - src/containers/mod.rs
autonomous: true
requirements:
  - PHASE-2-SCAFFOLDING
  - SC-1
  - SC-2
  - SC-3

must_haves:
  truths:
    - "SC-1: sbx::argv emits >=10 distinct argv shapes across build_create_args / build_exec_args / build_ports_args, verifiable by `cargo test -p agent-of-empires sbx::argv::tests`"
    - "SC-2: SbxRuntime::capabilities() returns the locked 7-flag sbx matrix (false x6, supports_dynamic_port_publish=true), structurally equal to RuntimeBase::SBX.capabilities, verifiable by unit test"
    - "SC-3: SbxRuntime::is_available() returns true for an injected chmod-+x'd tempfile binary and false for a nonexistent path, verifiable by two unit tests"
  artifacts:
    - path: "src/containers/sbx/mod.rs"
      provides: "SbxRuntime peer struct with injectable binary: PathBuf, ::new(), ::is_available(), ::capabilities()"
      contains: "pub(crate) struct SbxRuntime"
    - path: "src/containers/sbx/argv.rs"
      provides: "Three pure free-function argv builders: build_create_args, build_exec_args, build_ports_args"
      exports: ["build_create_args", "build_exec_args", "build_ports_args"]
    - path: "src/containers/mod.rs"
      provides: "Module declaration for the new sbx sub-module"
      contains: "pub(crate) mod sbx"
  key_links:
    - from: "src/containers/sbx/mod.rs::SbxRuntime::capabilities"
      to: "src/containers/runtime_base.rs::RuntimeBase::SBX::capabilities"
      via: "direct const access (route-through, not re-declared, per D-12 recommendation)"
      pattern: "RuntimeBase::SBX.capabilities"
    - from: "src/containers/sbx/argv.rs"
      to: "src/containers/container_interface.rs"
      via: "imports ContainerConfig + EnvEntry"
      pattern: "use crate::containers::container_interface::\\{ContainerConfig, EnvEntry\\}"
    - from: "src/containers/sbx/argv.rs::build_exec_args"
      to: "src/containers/runtime_base.rs:276-287"
      via: "verbatim copy of EnvEntry encoding pattern (Inherit -> KEY only, Literal -> KEY=VALUE)"
      pattern: "EnvEntry::Inherit.*key.clone|EnvEntry::Literal.*format"

risks:
  - id: R-01
    description: "Roadmap SC-4 mentions `RuntimeHandle::Sbx(SbxRuntime)` variant, but D-01 locks the facade dispatch via existing `RuntimeKind::Sbx` arms. Stale wording; the working mechanism is the existing dispatch. Coverage of SC-4 lives in plan 02-02; this plan only delivers SC-1/SC-2/SC-3."
---

<objective>
Stand up `src/containers/sbx/` as a private sub-module with two files: `mod.rs` (SbxRuntime peer struct with an injectable binary path, plus `is_available` and `capabilities` methods) and `argv.rs` (three pure free-function argv builders that compose `sbx create shell`, `sbx exec`, and `sbx ports --publish` invocations). Register the new module in `src/containers/mod.rs`. All work is pure / locally-testable; no `sbx` binary required.

Purpose: Phase 5 will consume these argv builders for real subprocess invocation. Plan 02-02 wires three dispatch arms in `runtime.rs` to route through this module. This plan delivers Roadmap SC-1, SC-2, and SC-3 from the Phase 2 success-criteria set.

Output: 2 new source files (`src/containers/sbx/mod.rs`, `src/containers/sbx/argv.rs`), 1 modified file (`src/containers/mod.rs` gains one `pub(crate) mod sbx;` line), and >=15 in-module unit tests (>=10 argv-shape tests across the three builders, plus 2 is_available tests, plus 1 capabilities test).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-sbxruntime-skeleton-and-pure-argv-builders/02-CONTEXT.md
@.planning/phases/02-sbxruntime-skeleton-and-pure-argv-builders/02-RESEARCH.md
@.planning/phases/02-sbxruntime-skeleton-and-pure-argv-builders/02-PATTERNS.md
@.planning/phases/02-sbxruntime-skeleton-and-pure-argv-builders/02-VALIDATION.md
@.planning/phases/01-capability-surface-and-trait-foundation/01-CONTEXT.md
@AGENTS.md
@src/containers/runtime_base.rs
@src/containers/container_interface.rs
@src/containers/mod.rs
@.planning/sbx-cli-reference.md

<interfaces>
Key types and exports the executor needs. Extracted from src/containers/container_interface.rs.

```rust
// src/containers/container_interface.rs
pub struct VolumeMount {
    pub host_path: String,
    pub container_path: String,
    pub read_only: bool,
}

#[derive(Debug, Clone, PartialEq)]
pub enum EnvEntry {
    Inherit { key: String, value: String },
    Literal { key: String, value: String },
}

pub struct ContainerConfig {
    pub working_dir: String,
    pub volumes: Vec<VolumeMount>,
    pub anonymous_volumes: Vec<String>,
    pub environment: Vec<EnvEntry>,
    pub cpu_limit: Option<String>,
    pub memory_limit: Option<String>,
    pub port_mappings: Vec<String>,
}

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct RuntimeCapabilities {
    pub supports_read_only_volumes: bool,
    pub supports_remove_volumes: bool,
    pub supports_port_publish_at_create: bool,
    pub supports_image_pull: bool,
    pub supports_anonymous_volumes: bool,
    pub supports_arbitrary_volume_paths: bool,
    pub supports_dynamic_port_publish: bool,
}
```

From src/containers/runtime_base.rs (the SBX capability source of truth, lines 99-119):

```rust
pub const SBX: Self = Self {
    binary: "sbx",
    name: "Docker Sandboxes",
    daemon_check_args: &["version"],
    pull_prefix: &[],
    remove_subcommand: "rm",
    capabilities: RuntimeCapabilities {
        supports_read_only_volumes: false,
        supports_remove_volumes: false,
        supports_port_publish_at_create: false,
        supports_image_pull: false,
        supports_anonymous_volumes: false,
        supports_arbitrary_volume_paths: false,
        supports_dynamic_port_publish: true,
    },
};
```

Existing `src/containers/mod.rs` module declarations (lines 1-4):

```rust
pub mod container_interface;
pub mod error;
mod runtime;
pub(crate) mod runtime_base;
```

EnvEntry encoding pattern verbatim from `src/containers/runtime_base.rs:276-287` (COPY THIS into build_exec_args; do NOT extract a shared helper, per RESEARCH.md anti-patterns):

```rust
for entry in &config.environment {
    args.push("-e".to_string());
    match entry {
        EnvEntry::Inherit { key, .. } => {
            // Only the key in argv; value stays in process env
            args.push(key.clone());
        }
        EnvEntry::Literal { key, value } => {
            args.push(format!("{}={}", key, value));
        }
    }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create src/containers/sbx/mod.rs with SbxRuntime peer struct + register module in containers/mod.rs</name>
  <files>src/containers/sbx/mod.rs, src/containers/mod.rs</files>
  <read_first>
    - src/containers/runtime_base.rs (lines 1-3 imports, 11-26 struct shape, 99-119 SBX const, 121-131 Command + --version probe — `is_available` and `capabilities` are direct adaptations of this analog)
    - src/containers/container_interface.rs (RuntimeCapabilities struct definition, line 59-86)
    - src/containers/mod.rs (lines 1-4 — the existing module declarations; new `pub(crate) mod sbx;` slots alphabetically after `runtime_base`)
    - .planning/phases/02-sbxruntime-skeleton-and-pure-argv-builders/02-CONTEXT.md (D-10 binary injection seam, D-12 capability routing)
    - .planning/phases/02-sbxruntime-skeleton-and-pure-argv-builders/02-PATTERNS.md (mod.rs section: lines 17-130 covers struct shape, is_available probe, capability routing, and in-module test pattern)
    - .planning/phases/02-sbxruntime-skeleton-and-pure-argv-builders/02-RESEARCH.md (Pattern 1 implementation details)
    - AGENTS.md ("Use unit tests in-module (#[cfg(test)]) for pure logic"; "No dead code"; "No emdashes or `--`")
  </read_first>
  <behavior>
    - Test 1 (capabilities_returns_locked_sbx_matrix): `SbxRuntime::new().capabilities()` returns a `RuntimeCapabilities` value structurally equal to `RuntimeBase::SBX.capabilities`; per-flag asserts (one assertion per line, in the same order as `test_sbx_capability_matrix` in runtime.rs:514-525) — supports_read_only_volumes = false, supports_remove_volumes = false, supports_port_publish_at_create = false, supports_image_pull = false, supports_anonymous_volumes = false, supports_arbitrary_volume_paths = false, supports_dynamic_port_publish = true.
    - Test 2 (is_available_with_injected_fake_binary_returns_true): A `tempfile::NamedTempFile` is written with `"#!/bin/sh\nexit 0\n"`, chmod'd to `0o755` via `std::os::unix::fs::PermissionsExt::set_mode`, then a `SbxRuntime { binary: tmp.path().to_path_buf() }` is constructed and `.is_available()` is asserted true. The tempfile is held in scope past the assertion (`let _tmp = ...`).
    - Test 3 (is_available_with_missing_binary_returns_false): `SbxRuntime { binary: PathBuf::from("/nonexistent/sbx-binary") }.is_available()` returns false.
  </behavior>
  <action>
    Create the new file `src/containers/sbx/mod.rs` containing exactly the following surface (no extras; per D-04 / D-06 action verbs are deferred):

    1. Imports: `std::path::PathBuf`, `std::process::Command`, `crate::containers::container_interface::RuntimeCapabilities`, `crate::containers::runtime_base::RuntimeBase`.
    2. Declare submodule: `pub(crate) mod argv;` (the argv free functions live in `argv.rs`, added by Task 2; declaring it here lets Task 2's tests compile when run alongside Task 1's tests — Task 2 may finalize the declaration, but adding it now keeps the module tree valid even if Task 2 is delayed).
    3. Define `pub(crate) struct SbxRuntime` with one field: `pub(crate) binary: PathBuf`. No derives.
    4. Impl `SbxRuntime` with three methods:
       - `pub fn new() -> Self` returning `Self { binary: PathBuf::from("sbx") }`. `Command::new(PathBuf::from("sbx"))` resolves bare names against `PATH`, matching `RuntimeBase::command()` behavior at runtime_base.rs:121-123.
       - `pub fn is_available(&self) -> bool` inlining the runtime_base.rs:125-131 probe: `Command::new(&self.binary).arg("--version").output().map(|o| o.status.success()).unwrap_or(false)`. Do not extract a shared helper; one call site.
       - `pub fn capabilities(&self) -> RuntimeCapabilities` returning `RuntimeBase::SBX.capabilities` (single source of truth, per D-12 RESEARCH.md recommendation; do NOT re-declare the literal).
    5. Add a `#[cfg(test)] mod tests` block at the bottom of `mod.rs` containing the three tests described in `<behavior>`. The Unix `PermissionsExt` import goes inside the test module; no `#[cfg(unix)]` guard needed (PLAT-01 says Windows is out of scope; AGENTS.md forbids scattered `#[cfg(target_os = ...)]` outside `src/process/`).
    6. Modify `src/containers/mod.rs` by adding exactly one line, `pub(crate) mod sbx;`, alphabetically after `pub(crate) mod runtime_base;` (line 4). Do not change any other line in `mod.rs`.

    Do not derive `Debug`, `Clone`, or any other trait on `SbxRuntime` until a downstream consumer requires it (AGENTS.md "No dead code"). Do not add doc comments that restate the code; if a comment is needed, explain the non-obvious "why" only (e.g., why `binary` is `PathBuf` instead of `&'static str` — test-injection seam D-10).
  </action>
  <verify>
    <automated>cargo fmt --check && cargo clippy -- -D warnings && cargo test -p agent-of-empires sbx::tests --lib</automated>
  </verify>
  <acceptance_criteria>
    - File `src/containers/sbx/mod.rs` exists.
    - `grep -c 'pub(crate) struct SbxRuntime' src/containers/sbx/mod.rs` returns 1.
    - `grep -c 'pub(crate) binary: PathBuf' src/containers/sbx/mod.rs` returns 1.
    - `grep -c 'RuntimeBase::SBX.capabilities' src/containers/sbx/mod.rs` returns >= 1 (the body of `capabilities()`).
    - `grep -c 'pub(crate) mod argv' src/containers/sbx/mod.rs` returns 1.
    - `grep -c 'pub(crate) mod sbx' src/containers/mod.rs` returns 1.
    - Filtering comments out of mod.rs proves no Phase 1 sentinel string remains: `grep -v '^\s*//' src/containers/sbx/mod.rs | grep -c 'Phase 1 stub'` returns 0.
    - No `#[allow(dead_code)]` introduced: `grep -c 'allow(dead_code)' src/containers/sbx/mod.rs` returns 0.
    - `cargo fmt --check` exits 0.
    - `cargo clippy -- -D warnings` exits 0.
    - `cargo test -p agent-of-empires sbx::tests --lib` exits 0 and the test count includes at least three sbx::tests entries.
    - All three named tests pass: `cargo test -p agent-of-empires sbx::tests::capabilities_returns_locked_sbx_matrix --lib` exits 0; `cargo test -p agent-of-empires sbx::tests::is_available_with_injected_fake_binary_returns_true --lib` exits 0; `cargo test -p agent-of-empires sbx::tests::is_available_with_missing_binary_returns_false --lib` exits 0.
  </acceptance_criteria>
  <done>SbxRuntime peer struct exists in `src/containers/sbx/mod.rs` with an injectable `binary: PathBuf`, `new()`, `is_available()`, and `capabilities()`. Module is registered in `src/containers/mod.rs`. Three in-module tests pass: capabilities matches `RuntimeBase::SBX.capabilities`, is_available is true for an injected chmod-+x'd tempfile binary, is_available is false for a nonexistent path. cargo fmt + cargo clippy + cargo test all green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create src/containers/sbx/argv.rs with three pure builders + >=10-shape test suite</name>
  <files>src/containers/sbx/argv.rs</files>
  <read_first>
    - src/containers/runtime_base.rs (lines 223-317: the `build_create_args` analog — structural shape of pure `Vec<String>` builder; lines 276-287: the EnvEntry encoding to COPY VERBATIM into `build_exec_args`)
    - src/containers/container_interface.rs (lines 1-50: `VolumeMount`, `EnvEntry`, `ContainerConfig` shapes — the inputs to all three builders)
    - .planning/phases/02-sbxruntime-skeleton-and-pure-argv-builders/02-CONTEXT.md (D-07 builder signatures, D-08 kit_path semantics, D-09 `&ContainerConfig`, `<specifics>` block for argv skeletons of all three subcommands)
    - .planning/phases/02-sbxruntime-skeleton-and-pure-argv-builders/02-PATTERNS.md (lines 134-292: argv.rs section with full signatures and EnvEntry encoding)
    - .planning/phases/02-sbxruntime-skeleton-and-pure-argv-builders/02-RESEARCH.md (Pattern 1 and Pattern 2 implementation walkthroughs; Phase 2 sbx::argv code blocks)
    - .planning/sbx-cli-reference.md (`sbx create`, `sbx exec`, `sbx ports` argv shapes; positional vs flag args; `:ro` suffix rules)
    - AGENTS.md ("No dead code"; "No emdashes or `--`"; "Use unit tests in-module (#[cfg(test)]) for pure logic")
  </read_first>
  <behavior>
    Test suite must cover at least 10 distinct argv shapes across the three builders (Roadmap SC-1). Use these named tests; each is a single fact about argv composition.

    build_create_args tests (>=6 distinct shapes, function names prefixed `create_args_`):
    - create_args_minimal: name + image, no kit, no cpu/mem, empty volumes/env/ports/anonymous_volumes. Argv begins with ["create", "shell", "--name", <name>, "--template", <image>]; no `--kit`, no `--cpus`, no `-m`, no positional after image.
    - create_args_with_kit: kit_path = Some(Path::new("/var/cache/aoe/sbx-kit/v1")). Argv contains "--kit" immediately followed by "/var/cache/aoe/sbx-kit/v1" between `--name <name>` and `--template <image>`.
    - create_args_with_cpus_and_memory: cpu_limit = Some("2"), memory_limit = Some("4g"). Argv contains "--cpus" "2" and "-m" "4g" in that order, after `--template <image>`.
    - create_args_with_volume_no_ro: volumes = one VolumeMount with host_path="/work/proj", container_path="/work/proj", read_only=false. Argv last token is "/work/proj" (no `:ro` suffix).
    - create_args_with_volume_ro: volumes = one VolumeMount with host_path="/work/proj", container_path="/work/proj", read_only=true. Argv last token is "/work/proj:ro".
    - create_args_with_multiple_volumes: volumes = two mounts; both positional after image, in declared order, each with correct `:ro` suffix per its flag.
    - create_args_skips_anonymous_volumes: anonymous_volumes = vec!["/tmp/cache"]. Argv does NOT contain "/tmp/cache" anywhere (sbx supports_anonymous_volumes=false; unconditional skip; no warning needed at this layer per RESEARCH.md anti-patterns).
    - create_args_skips_port_mappings: port_mappings = vec!["3000:3000"]. Argv does NOT contain "-p" and does NOT contain "3000:3000".

    build_exec_args tests (>=6 distinct shapes covering at least the {interactive, tty, both, neither} x {workdir, no workdir} x {env present, env absent} combinations, function names prefixed `exec_args_`):
    - exec_args_minimal: interactive=false, tty=false, workdir=None, env=&[], cmd=&["sh", "-c", "echo hi"]. Argv = ["exec", <name>, "sh", "-c", "echo hi"]. No -i / -t / -w / -e tokens anywhere.
    - exec_args_interactive_only: interactive=true, tty=false. Argv contains "-i" but not "-t". `-i` appears before <name>.
    - exec_args_tty_only: interactive=false, tty=true. Argv contains "-t" but not "-i". `-t` appears before <name>.
    - exec_args_interactive_and_tty: interactive=true, tty=true. Argv contains "-i" and "-t", "-i" before "-t" (insertion order), both before <name>.
    - exec_args_with_workdir: workdir=Some("/workspace/proj"). Argv contains "-w" followed by "/workspace/proj", emitted after the i/t flags and before env entries.
    - exec_args_with_env_inherit: env=&[EnvEntry::Inherit { key: "GITHUB_TOKEN".to_string(), value: "ignored".to_string() }]. Argv contains "-e" followed by "GITHUB_TOKEN" (NO `=value`, mirrors runtime_base.rs:279-282). Value field is not in argv.
    - exec_args_with_env_literal: env=&[EnvEntry::Literal { key: "FOO".to_string(), value: "bar".to_string() }]. Argv contains "-e" followed by "FOO=bar" (matches runtime_base.rs:283-285).
    - exec_args_with_mixed_env: env contains both Inherit and Literal entries. Argv contains a "-e" pair for each, in declared order, each encoded per the rules above.
    - exec_args_full_permutation: interactive=true, tty=true, workdir=Some("/w"), env with one Inherit + one Literal, cmd=&["claude", "--continue"]. Argv exact ordering = ["exec", "-i", "-t", "-w", "/w", "-e", <inherit key>, "-e", <literal kv>, <name>, "claude", "--continue"].

    build_ports_args tests (>=3 shapes, function names prefixed `ports_args_`):
    - ports_args_simple: port_spec="3000:3000". Argv = ["ports", <name>, "--publish", "3000:3000"].
    - ports_args_with_host_ip: port_spec="127.0.0.1:8080:80". Argv = ["ports", <name>, "--publish", "127.0.0.1:8080:80"].
    - ports_args_with_protocol: port_spec="3000:3000/tcp". Argv = ["ports", <name>, "--publish", "3000:3000/tcp"]. Builder is a pass-through; no spec parsing.

    Total test count: >=17 (eight create_args_*, eight exec_args_*, three ports_args_*); SC-1 minimum is 10.
  </behavior>
  <action>
    Create `src/containers/sbx/argv.rs` with exactly three `pub fn` free functions and a `#[cfg(test)] mod tests` block.

    Imports for the file: `std::path::Path`, `crate::containers::container_interface::{ContainerConfig, EnvEntry}`. Do NOT import `VolumeMount` (it travels through `ContainerConfig::volumes: Vec<VolumeMount>` and is accessed via field projection inside the function). Do NOT import `RuntimeBase`.

    Function 1 — `pub fn build_create_args(name: &str, image: &str, kit_path: Option<&Path>, config: &ContainerConfig) -> Vec<String>` (signature locked by D-07). Emission order per CONTEXT.md `<specifics>`:
    1. Push "create", "shell" (positional subcommand prefix).
    2. Push "--name", name.to_string().
    3. If `kit_path` is `Some(kp)`: push "--kit" and `kp.display().to_string()`.
    4. Push "--template", image.to_string().
    5. If `config.cpu_limit` is `Some(cpu)`: push "--cpus" and `cpu.clone()`.
    6. If `config.memory_limit` is `Some(mem)`: push "-m" and `mem.clone()`.
    7. For each `vol in &config.volumes`: push `format!("{}{}", vol.host_path, if vol.read_only { ":ro" } else { "" })`. Use `vol.host_path` (NOT `container_path`; Phase 3 enforces equality, but Phase 2 emits the host path verbatim per RESEARCH.md "Deferred").
    8. Do NOT emit `config.anonymous_volumes` (sbx supports_anonymous_volumes=false; unconditional skip; no warn-log here — that lives in the caller layer in Phase 3 if it lands).
    9. Do NOT emit `config.port_mappings` (sbx supports_port_publish_at_create=false; Phase 5 emits ports out-of-band via `build_ports_args`).
    10. Do NOT emit `config.environment` (sbx create doesn't accept `-e` per the sbx CLI reference; env entries go to `sbx exec` only).

    Function 2 — `pub fn build_exec_args(name: &str, workdir: Option<&str>, env: &[EnvEntry], interactive: bool, tty: bool, cmd: &[&str]) -> Vec<String>` (signature locked by D-07). Emission order:
    1. Push "exec".
    2. If `interactive`: push "-i".
    3. If `tty`: push "-t".
    4. If `workdir` is `Some(wd)`: push "-w" and `wd.to_string()`.
    5. For each `entry in env`: push "-e", then match — `EnvEntry::Inherit { key, .. }` -> push `key.clone()`; `EnvEntry::Literal { key, value }` -> push `format!("{}={}", key, value)`. This MUST be a verbatim copy of the runtime_base.rs:276-287 pattern (modulo the wrapping condition); do NOT factor out a shared helper. RESEARCH.md anti-patterns forbids it at this stage.
    6. Push name.to_string().
    7. For each `c in cmd`: push `(*c).to_string()`.

    Function 3 — `pub fn build_ports_args(name: &str, port_spec: &str) -> Vec<String>` (signature locked by D-07). Body is a single literal: `vec!["ports".to_string(), name.to_string(), "--publish".to_string(), port_spec.to_string()]`. No parsing of `port_spec`; it passes through verbatim.

    Test module: a single `#[cfg(test)] mod tests { use super::*; ... }` block at the bottom of the file. Each test constructs the inputs locally (do not share fixtures across tests; AGENTS.md "tests must be deterministic"). Use the named tests from `<behavior>` exactly so the SC-1 verify command `cargo test -p agent-of-empires sbx::argv::tests::create_args_` / `..._exec_args_` / `..._ports_args_` matches.

    For `ContainerConfig` construction inside tests, use the existing public field shape from `container_interface.rs:42-50` (no builder needed; struct literal with all 7 fields). For `VolumeMount`, same.
  </action>
  <verify>
    <automated>cargo fmt --check && cargo clippy -- -D warnings && cargo test -p agent-of-empires sbx::argv::tests --lib</automated>
  </verify>
  <acceptance_criteria>
    - File `src/containers/sbx/argv.rs` exists.
    - `grep -c 'pub fn build_create_args' src/containers/sbx/argv.rs` returns 1.
    - `grep -c 'pub fn build_exec_args' src/containers/sbx/argv.rs` returns 1.
    - `grep -c 'pub fn build_ports_args' src/containers/sbx/argv.rs` returns 1.
    - `grep -c 'kit_path: Option<&Path>' src/containers/sbx/argv.rs` returns 1.
    - `grep -c 'EnvEntry::Inherit' src/containers/sbx/argv.rs` returns at least 1 (in `build_exec_args`).
    - `grep -c 'EnvEntry::Literal' src/containers/sbx/argv.rs` returns at least 1.
    - `grep -c 'anonymous_volumes' src/containers/sbx/argv.rs` returns 0 (no anonymous-volume emission in sbx).
    - `grep -c '"-p"' src/containers/sbx/argv.rs` returns 0 (no `-p` emission in `build_create_args`).
    - SC-1 minimum count met: `cargo test -p agent-of-empires sbx::argv::tests --lib -- --list 2>/dev/null | grep -E ': test$' | wc -l` returns >= 10. (Implementation note: rustc's `--list` flag prints one `module::path::test_name: test` line per test; the grep extracts those.)
    - `cargo test -p agent-of-empires sbx::argv::tests::create_args_ --lib` exits 0 and runs at least 6 tests.
    - `cargo test -p agent-of-empires sbx::argv::tests::exec_args_ --lib` exits 0 and runs at least 6 tests.
    - `cargo test -p agent-of-empires sbx::argv::tests::ports_args_ --lib` exits 0 and runs at least 3 tests.
    - `cargo fmt --check` exits 0.
    - `cargo clippy -- -D warnings` exits 0.
    - No `#[allow(dead_code)]` introduced: `grep -c 'allow(dead_code)' src/containers/sbx/argv.rs` returns 0.
  </acceptance_criteria>
  <done>`src/containers/sbx/argv.rs` exists with three `pub fn` free-function builders (`build_create_args`, `build_exec_args`, `build_ports_args`) matching the D-07 signatures. At least 17 in-module unit tests pass, covering >=10 distinct argv shapes across the three builders. EnvEntry encoding is a verbatim copy of the runtime_base.rs:276-287 pattern. cargo fmt + cargo clippy + cargo test all green.</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. Filesystem layout matches PATTERNS.md target:
   - `ls src/containers/sbx/` shows `mod.rs` and `argv.rs`.
   - `grep -c 'pub(crate) mod sbx' src/containers/mod.rs` returns 1.

2. SC-1 minimum (>=10 argv shapes) is met:
   - `cargo test -p agent-of-empires sbx::argv::tests --lib -- --list 2>/dev/null | grep -E ': test$' | wc -l` returns >= 10.

3. SC-2 (capabilities matches sbx matrix):
   - `cargo test -p agent-of-empires sbx::tests::capabilities_returns_locked_sbx_matrix --lib` exits 0.

4. SC-3 (is_available injection seam):
   - `cargo test -p agent-of-empires sbx::tests::is_available_with_injected_fake_binary_returns_true --lib` exits 0.
   - `cargo test -p agent-of-empires sbx::tests::is_available_with_missing_binary_returns_false --lib` exits 0.

5. No regression on existing tests:
   - `cargo test -p agent-of-empires --lib containers::` exits 0.

6. Repo policy:
   - `cargo fmt --check` exits 0.
   - `cargo clippy -- -D warnings` exits 0.
   - `grep -rE '(^|\s)(em-dash|--)\s' src/containers/sbx/ 2>/dev/null | grep -v '^\s*//' | grep -c '--'` returns 0 inside non-comment source (verifies no `--` separator in prose).
   - `grep -rc 'allow(dead_code)' src/containers/sbx/` returns 0.
</verification>

<success_criteria>
- `src/containers/sbx/mod.rs` and `src/containers/sbx/argv.rs` exist.
- `src/containers/mod.rs` has `pub(crate) mod sbx;` declared.
- `cargo test -p agent-of-empires sbx::` passes >= 13 tests (3 in `sbx::tests`, >= 10 in `sbx::argv::tests`; actually 17+ with the recommended set).
- `cargo test -p agent-of-empires --lib containers::` passes with no regression.
- `cargo fmt --check`, `cargo clippy -- -D warnings` both exit 0.
- SC-1 (>=10 argv shapes), SC-2 (sbx capability matrix), SC-3 (is_available injection seam) all verified by automated unit tests. SC-4 (dispatch routing) is delivered in plan 02-02.
</success_criteria>

<output>
Create `.planning/phases/02-sbxruntime-skeleton-and-pure-argv-builders/02-01-SUMMARY.md` when done.
</output>
